diff --git a/src/graphics/host_gpu/renderer/cache/textureCache.cpp b/src/graphics/host_gpu/renderer/cache/textureCache.cpp
index 35e4c7a..b1b53a2 100644
--- a/src/graphics/host_gpu/renderer/cache/textureCache.cpp
+++ b/src/graphics/host_gpu/renderer/cache/textureCache.cpp
@@ -2186,19 +2186,16 @@ void TextureCache::RunGarbageCollector(uint64_t elapsed_submissions) {
 				continue;
 			}
 			if (owner->IsGpuModified()) {
-				const bool safe = SafeToDownload(*owner);
-				// The GPU image is the only current copy until a readback succeeds.
-				// Evicting an unresolvable image makes later texture reads lose pixels.
-				if (!safe || owner->info.IsTiled()) {
-					continue;
+				// A queued readback has not made guest pixels current yet. Do not
+				// drop the last native copy while the game may rebind this image.
+				static uint32_t preserved_reports = 0;
+				if (preserved_reports++ < 16) {
+					LOGF("Build32 texture GC preserved GPU image: addr=0x%016" PRIx64
+					     " tiled=%d render_target=%d storage=%d\n",
+					     owner->info.data.address, owner->info.IsTiled() ? 1 : 0,
+					     owner->usage.render_target ? 1 : 0, owner->usage.storage ? 1 : 0);
 				}
-				if (!pressured) {
-					continue;
-				}
-				if (!TryDownloadImage(id)) {
-					continue;
-				}
-				owner->ClearGpuModified();
+				continue;
 			}
 			DeleteImage(id);
 			if (m_total_used_memory < m_critical_gc_memory && aggressive) {
