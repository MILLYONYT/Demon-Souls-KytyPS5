diff --git a/src/graphics/host_gpu/renderer/cache/textureCache.cpp b/src/graphics/host_gpu/renderer/cache/textureCache.cpp
index 18d5b4b..0166f1f 100644
--- a/src/graphics/host_gpu/renderer/cache/textureCache.cpp
+++ b/src/graphics/host_gpu/renderer/cache/textureCache.cpp
@@ -518,22 +518,26 @@ TextureCache::ImageIds TextureCache::FindImagesInRegion(uint64_t address, uint64
 
 ImageId TextureCache::GetNullImage(const ImageDesc& desc) {
 	const auto format = desc.info.pixel_format;
-	if (const auto found = m_null_images.find(format); found != m_null_images.end()) {
+	const auto key = static_cast<uint64_t>(static_cast<uint32_t>(format)) |
+	                 (static_cast<uint64_t>(static_cast<uint32_t>(desc.info.type)) << 32) |
+	                 (static_cast<uint64_t>(desc.info.samples) << 48);
+	if (const auto found = m_null_images.find(key); found != m_null_images.end()) {
 		return found->second;
 	}
 	ImageInfo info {};
 	info.pixel_format    = desc.info.pixel_format;
 	info.guest_format    = desc.info.guest_format;
-	info.type            = Prospero::ImageType::kColor2D;
+	// Null 3D/1D descriptors still require an image of the matching Vulkan type.
+	info.type            = desc.info.type;
 	info.extent          = {1, 1, 1};
 	info.resources       = {1, 1};
 	info.pitch           = 1;
 	info.bytes_per_block = std::max(desc.info.bytes_per_block, 1u);
-	info.samples         = 1;
+	info.samples         = desc.info.samples;
 	info.tile_mode       = Prospero::TileMode::kLinear;
 	info.mip_layout[0]   = {0, info.bytes_per_block, 1, 1};
 	const auto id        = InsertImage(info);
-	m_null_images.emplace(format, id);
+	m_null_images.emplace(key, id);
 	return id;
 }
 
diff --git a/src/graphics/host_gpu/renderer/cache/textureCache.h b/src/graphics/host_gpu/renderer/cache/textureCache.h
index f373595..c75086b 100644
--- a/src/graphics/host_gpu/renderer/cache/textureCache.h
+++ b/src/graphics/host_gpu/renderer/cache/textureCache.h
@@ -168,7 +168,7 @@ private:
 	BufferCache&                                      m_buffer_cache;
 	Common::SlotVector<Image>                         m_slot_images;
 	ImagePageTable                                    m_image_page_table;
-	std::unordered_map<vk::Format, ImageId>           m_null_images;
+	std::unordered_map<uint64_t, ImageId>              m_null_images;
 	Common::LeastRecentlyUsedCache<ImageId, uint64_t> m_lru_cache;
 	std::unordered_set<ImageId>                       m_download_images;
 	std::map<uint64_t, MetaDataInfo>                  m_surface_metas;
diff --git a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp
index 83cabcd..559c569 100644
--- a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp
+++ b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp
@@ -100,8 +100,23 @@ void PipelineCacheLog(fmt::format_string<Args...> format, Args&&... args) {
 }
 
 bool ReadShaderGuestMemory(void*, uint64_t address, uint32_t* value) {
-	return value != nullptr &&
-	       Libs::LibKernel::Memory::TryReadGpuCleanBacking(address, value, sizeof(*value));
+	if (value == nullptr) {
+		return false;
+	}
+	if (Libs::LibKernel::Memory::TryReadGpuCleanBacking(address, value, sizeof(*value))) {
+		return true;
+	}
+	// After a reload a descriptor may still belong to a GPU-written buffer.
+	// Drain that owner before failing the clean read; never read stale backing.
+	if (Libs::LibKernel::Memory::SyncGpuCleanBacking(address, sizeof(*value)) &&
+	    Libs::LibKernel::Memory::TryReadGpuCleanBacking(address, value, sizeof(*value))) {
+		return true;
+	}
+	static std::atomic_uint rejected_reads {0};
+	if (rejected_reads.fetch_add(1, std::memory_order_relaxed) < 8) {
+		PipelineCacheLog("Shader clean descriptor read unavailable at 0x{:016x}", address);
+	}
+	return false;
 }
 
 bool SyncShaderGuestMemory(void*, uint64_t address, uint64_t size) {
@@ -119,8 +134,15 @@ bool ReadShaderRawGuestMemory(void*, uint64_t address, uint32_t* value) {
 }
 
 bool ReadShaderMemorySpan(void*, uint64_t address, uint32_t* values, uint32_t count, bool clean) {
-	return count >= 2 && count <= 16 &&
-	       Libs::LibKernel::Memory::TryReadGpuShaderSpan(address, values, count * 4u, clean);
+	if (count < 2 || count > 16) {
+		return false;
+	}
+	const auto size = count * 4u;
+	if (Libs::LibKernel::Memory::TryReadGpuShaderSpan(address, values, size, clean)) {
+		return true;
+	}
+	return clean && Libs::LibKernel::Memory::SyncGpuCleanBacking(address, size) &&
+	       Libs::LibKernel::Memory::TryReadGpuShaderSpan(address, values, size, true);
 }
 
 void ReportMaterialization(const char* label, ShaderType stage, uint64_t hash,
@@ -486,11 +508,6 @@ void PipelineCache::InitializeDriverCache() {
 		PipelineCacheLog("Vulkan pipeline cache: disabled (unknown git revision)");
 		return;
 	}
-	if (git_hash.ends_with("-dirty")) {
-		PipelineCacheLog("Vulkan pipeline cache: disabled (dirty build)");
-		return;
-	}
-
 	m_driver_cache_path     = std::filesystem::path("_PipelineCache") / (title_id + ".bin");
 	const auto path         = Common::PathToString(m_driver_cache_path);
 	const bool cache_exists = Common::File::IsFileExisting(m_driver_cache_path);
@@ -559,6 +576,25 @@ void PipelineCache::InitializeDriverCache() {
 
 void PipelineCache::Save() {
 	Common::LockGuard lock(m_mutex);
+	SaveDriverCacheLocked(true);
+}
+
+void PipelineCache::CheckpointDriverCacheLocked() {
+	if (m_driver_cache == nullptr || ++m_new_pipelines_since_checkpoint < 16) {
+		return;
+	}
+	const auto now = std::chrono::steady_clock::now();
+	if (now - m_last_driver_cache_checkpoint < std::chrono::seconds(90)) {
+		return;
+	}
+	// A cache snapshot can stall the renderer. Limit snapshots to active periods
+	// with new pipelines and leave the in-memory cache available for this session.
+	m_new_pipelines_since_checkpoint = 0;
+	m_last_driver_cache_checkpoint = now;
+	SaveDriverCacheLocked(false);
+}
+
+void PipelineCache::SaveDriverCacheLocked(bool final) {
 	if (m_driver_cache == nullptr) {
 		return;
 	}
@@ -612,8 +648,10 @@ void PipelineCache::Save() {
 	}
 	PipelineCacheLog("Vulkan pipeline cache: saved {} bytes to {}", payload.size(),
 	                 Common::PathToString(m_driver_cache_path));
-	m_graphics.device.destroyPipelineCache(m_driver_cache, nullptr);
-	m_driver_cache = nullptr;
+	if (final) {
+		m_graphics.device.destroyPipelineCache(m_driver_cache, nullptr);
+		m_driver_cache = nullptr;
+	}
 }
 
 PipelineCache::GraphicsPrograms PipelineCache::GetGraphicsPrograms(
@@ -850,6 +888,7 @@ PipelineCache::Pipeline& PipelineCache::CreateGraphicsPipeline(
 
 	auto [iter, inserted] = m_graphics_pipelines.emplace(std::move(key), std::move(cached));
 	EXIT_IF(!inserted);
+	CheckpointDriverCacheLocked();
 
 	return *iter->second;
 }
@@ -880,6 +919,7 @@ PipelineCache::CreateComputePipeline(const ShaderComputeInputInfo& input_info,
 
 	auto [iter, inserted] = m_compute_pipelines.emplace(compute_program.id, std::move(cached));
 	EXIT_IF(!inserted);
+	CheckpointDriverCacheLocked();
 
 	return *iter->second;
 }
diff --git a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h
index 6fa19e7..e517c2e 100644
--- a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h
+++ b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h
@@ -9,6 +9,7 @@
 #include "graphics/host_gpu/vulkanCommon.h"
 #include "graphics/shader/shader.h"
 
+#include <chrono>
 #include <cstddef>
 #include <filesystem>
 #include <memory>
@@ -211,12 +212,17 @@ private:
 	std::unique_ptr<ProgramCache> m_program_cache;
 	vk::PipelineCache             m_driver_cache = nullptr;
 	std::filesystem::path         m_driver_cache_path;
+	uint32_t                      m_new_pipelines_since_checkpoint = 0;
+	std::chrono::steady_clock::time_point m_last_driver_cache_checkpoint =
+	    std::chrono::steady_clock::now();
 	std::unordered_map<GraphicsPipelineKey, std::unique_ptr<Pipeline>, GraphicsPipelineKeyHash>
 	                                                        m_graphics_pipelines;
 	std::unordered_map<uint64_t, std::unique_ptr<Pipeline>> m_compute_pipelines;
 	Common::Mutex m_mutex;
 
 	void InitializeDriverCache();
+	void SaveDriverCacheLocked(bool final);
+	void CheckpointDriverCacheLocked();
 };
 
 void LogPipelineTrace(const char* phase, uint64_t vertex_program_id, uint64_t pixel_program_id);
diff --git a/src/graphics/host_gpu/vma.cpp b/src/graphics/host_gpu/vma.cpp
index f7b512f..3deb1fa 100644
--- a/src/graphics/host_gpu/vma.cpp
+++ b/src/graphics/host_gpu/vma.cpp
@@ -136,11 +136,29 @@ bool GraphicContext::CreateImage(const vk::ImageCreateInfo& image_info, VulkanIm
 	alloc_info.requiredFlags = VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT;
 
 	vk::Image::CType native_image = VK_NULL_HANDLE;
-	const auto        result       = static_cast<vk::Result>(
+	auto              result       = static_cast<vk::Result>(
 	    vmaCreateImage(allocator, static_cast<const vk::ImageCreateInfo::NativeType*>(image_info),
 	                   &alloc_info, &native_image, &image.allocation, nullptr));
+	if (result == vk::Result::eErrorOutOfDeviceMemory ||
+	    result == vk::Result::eErrorOutOfHostMemory) {
+		// Prefer the original device-local allocation. If it is exhausted, let VMA
+		// choose another memory type supported by this image before giving up.
+		LOGF("vmaCreateImage: %s; retrying with device-local memory preferred\n",
+		     vk::to_string(result).c_str());
+		native_image        = VK_NULL_HANDLE;
+		image.allocation    = nullptr;
+		alloc_info.requiredFlags  = 0;
+		alloc_info.preferredFlags = VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT;
+		result = static_cast<vk::Result>(vmaCreateImage(
+		    allocator, static_cast<const vk::ImageCreateInfo::NativeType*>(image_info),
+		    &alloc_info, &native_image, &image.allocation, nullptr));
+	}
 	image.image = native_image;
 	if (result != vk::Result::eSuccess) {
+		LOGF("vmaCreateImage failed: %s, format=%d extent=%ux%ux%u layers=%u levels=%u\n",
+		     vk::to_string(result).c_str(), static_cast<int>(image_info.format),
+		     image_info.extent.width, image_info.extent.height, image_info.extent.depth,
+		     image_info.arrayLayers, image_info.mipLevels);
 		LogMemoryBudget();
 		return false;
 	}
diff --git a/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp b/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp
index bf1c28c..d398b24 100644
--- a/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp
+++ b/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp
@@ -1,11 +1,13 @@
 #include "graphics/shader/recompiler/ir/passes/ResourceMaterialization.h"
 
 #include "common/assert.h"
+#include "common/logging/log.h"
 #include "graphics/guest_gpu/gpu_format.h"
 #include "graphics/shader/recompiler/ir/ShaderIR.h"
 #include "graphics/shader/shaderBindings.h"
 
 #include <algorithm>
+#include <atomic>
 #include <array>
 #include <bit>
 #include <cstdio>
@@ -622,6 +624,19 @@ static bool MaterializeSnapshot(const ResourcePlan& program, const SrtRuntime& r
 			clean_runtime.read_memory = runtime.read_specialization_memory;
 			std::vector<DescriptorValue> tables;
 			if (!EvaluateDescriptorSources(program, requests, clean_runtime, tables)) {
+				if (program.stage == ShaderType::Compute &&
+				    program.shader_hash == 0x05021f35c669b111ull) {
+					// During reload this table can be unavailable while the guest rebuilds
+					// its heap. Match the inactive-source null-image path for this shader;
+					// the next dispatch will read the table again.
+					static std::atomic_bool logged_unavailable_table {false};
+					if (!logged_unavailable_table.exchange(true, std::memory_order_relaxed)) {
+						LOGF("SRT indirect image table unavailable: shader=0x%016llx image=%u; using null image\n",
+						     static_cast<unsigned long long>(program.shader_hash), image_index);
+					}
+					next.images[image_index].dword_count = 8u;
+				continue;
+				}
 				return fail("an indirect image table source did not evaluate");
 			}
 			IndirectImage table;
diff --git a/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp b/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp
index 2aedc25..6a24d1b 100644
--- a/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp
+++ b/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp
@@ -1,11 +1,18 @@
+#ifndef NOMINMAX
+#define NOMINMAX
+#endif
+
 #include "graphics/shader/recompiler/ir/passes/LinearSrt.h"
 #include "graphics/shader/recompiler/ir/passes/SrtWalker.h"
 
 #include "common/assert.h"
+#include "common/logging/log.h"
 #include "graphics/shader/recompiler/ir/ShaderIR.h"
 
 #include <algorithm>
+#include <atomic>
 #include <cstdlib>
+#include <cstdio>
 #include <cstring>
 #include <bit>
 #include <cmath>
@@ -1173,10 +1180,14 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
                                 std::span<const uint8_t> clean_flat_slots,
                                 std::vector<uint8_t>& active_sources) {
 	if (!program.srt_plan_complete) {
+		LOGF("SRT evaluation missing plan: shader=0x%016llx\n",
+		     static_cast<unsigned long long>(program.shader_hash));
 		return false;
 	}
 	if (std::ranges::any_of(clean_flat_slots, [](uint8_t clean) { return clean != 0u; }) &&
 	    runtime.read_specialization_memory == nullptr) {
+		LOGF("SRT evaluation missing specialization reader: shader=0x%016llx\n",
+		     static_cast<unsigned long long>(program.shader_hash));
 		return false;
 	}
 	SrtRuntime clean_runtime  = runtime;
@@ -1190,6 +1201,8 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
 	for (const auto source_index: sources) {
 		const auto* source = Source(program, source_index);
 		if (source == nullptr) {
+			LOGF("SRT evaluation missing source: shader=0x%016llx source=%u\n",
+			     static_cast<unsigned long long>(program.shader_hash), source_index);
 			return false;
 		}
 		DescriptorValue value;
@@ -1197,7 +1210,31 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
 		if (!evaluate_flat || active[source_index]) {
 			for (uint32_t index = 0; index < source->dword_count; index++) {
 				if (!evaluator.Evaluate(source->dwords[index], value.dwords[index])) {
-					return false;
+					const bool reload_shader = evaluate_flat &&
+					    ((program.stage == ShaderType::Pixel &&
+					      (program.shader_hash == 0x06f5c0838a0b409bull ||
+					      program.shader_hash == 0xe1f1578df1050d8bull)) ||
+					     (program.stage == ShaderType::Compute &&
+					      program.shader_hash == 0x05021f35c669b111ull));
+					static std::atomic_bool logged_pixel {false};
+					static std::atomic_bool logged_compute {false};
+					auto& logged = program.stage == ShaderType::Pixel ? logged_pixel : logged_compute;
+					if (!reload_shader || !logged.exchange(true, std::memory_order_relaxed)) {
+						LOGF("SRT descriptor evaluation failed: shader=0x%016llx source=%u word=%u%s\n",
+						     static_cast<unsigned long long>(program.shader_hash), source_index, index,
+						     reload_shader ? "; using null descriptor" : "");
+						std::printf("SRT descriptor evaluation failed: shader=0x%016llx source=%u word=%u%s\n",
+						            static_cast<unsigned long long>(program.shader_hash), source_index, index,
+						            reload_shader ? "; using null descriptor" : "");
+						std::fflush(stdout);
+					}
+					if (!reload_shader) {
+						return false;
+					}
+					// On reload an unavailable descriptor cannot be used safely. Bind the
+					// existing null resource until the game supplies a readable one.
+					value.dwords.fill(0);
+					break;
 				}
 			}
 		}
@@ -1210,10 +1247,33 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
 			const bool clean    = read.flat_offset < clean_flat_slots.size() &&
 			                      clean_flat_slots[read.flat_offset] != 0u;
 			auto&      selected = clean ? clean_evaluator : evaluator;
-			if (read.flat_offset >= flattened.size() ||
-			    !selected.Evaluate(read.value, flattened[read.flat_offset])) {
+			if (read.flat_offset >= flattened.size()) {
 				return false;
 			}
+			if (!selected.Evaluate(read.value, flattened[read.flat_offset])) {
+				const bool reload_shader =
+				    (program.stage == ShaderType::Pixel &&
+				     (program.shader_hash == 0x06f5c0838a0b409bull ||
+				     program.shader_hash == 0xe1f1578df1050d8bull)) ||
+				    (program.stage == ShaderType::Compute &&
+				     program.shader_hash == 0x05021f35c669b111ull);
+				static std::atomic_bool logged_pixel {false};
+				static std::atomic_bool logged_compute {false};
+				auto& logged = program.stage == ShaderType::Pixel ? logged_pixel : logged_compute;
+				if (!reload_shader || !logged.exchange(true, std::memory_order_relaxed)) {
+					LOGF("SRT flat read failed: shader=0x%016llx slot=%u clean=%u%s\n",
+					     static_cast<unsigned long long>(program.shader_hash), read.flat_offset,
+					     clean ? 1u : 0u, reload_shader ? "; using zero" : "");
+					std::printf("SRT flat read failed: shader=0x%016llx slot=%u clean=%u%s\n",
+					            static_cast<unsigned long long>(program.shader_hash), read.flat_offset,
+					            clean ? 1u : 0u, reload_shader ? "; using zero" : "");
+					std::fflush(stdout);
+				}
+				if (!reload_shader) {
+					return false;
+				}
+				flattened[read.flat_offset] = 0;
+			}
 		}
 	}
 	results = std::move(evaluated);
@@ -1279,6 +1339,16 @@ static std::shared_ptr<LinearSrtPlan> BuildControlledLinearSrt(const ResourcePla
 void BuildLinearSrtPlan(ResourcePlan& program) {
     program.linear_srt.reset();
 #if defined(__x86_64__) || defined(_M_X64)
+    // Diagnose the reload failures with the checked evaluator rather than
+    // executing the linear plan. Keep all other shaders on the fast path.
+    const bool reload_pixel = program.stage == ShaderType::Pixel &&
+                              (program.shader_hash == 0x06f5c0838a0b409bull ||
+                              program.shader_hash == 0xe1f1578df1050d8bull);
+    const bool reload_compute = program.stage == ShaderType::Compute &&
+                                program.shader_hash == 0x05021f35c669b111ull;
+    if (reload_pixel || reload_compute) {
+        return;
+    }
     auto compiled=std::make_shared<LinearSrtPlan>();
     if (LinearSrtCompiler(program,*compiled).Build()) program.linear_srt=std::move(compiled);
     else program.linear_srt=BuildControlledLinearSrt(program);
