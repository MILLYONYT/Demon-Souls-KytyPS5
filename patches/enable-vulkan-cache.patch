diff --git a/src/graphics/guest_gpu/graphicsRun.cpp b/src/graphics/guest_gpu/graphicsRun.cpp
index cca1005..f4f6dfd 100644
--- a/src/graphics/guest_gpu/graphicsRun.cpp
+++ b/src/graphics/guest_gpu/graphicsRun.cpp
@@ -23,7 +23,9 @@
 #include <algorithm>
 #include <array>
 #include <atomic>
+#include <chrono>
 #include <cstdio>
+#include <cstdlib>
 #include <cstring>
 #include <deque>
 #include <memory>
@@ -39,6 +41,10 @@ static thread_local Pm4Execution*     g_current_execution = nullptr;
 static thread_local bool              g_gpu_mutex_owned   = false;
 static thread_local bool              g_gpu_thread        = false;
 static thread_local GuestGpu*         g_gpu_state         = nullptr;
+#if defined(KYTY_B32_PHASE_PROFILE)
+// Only the GPU worker writes these; report their totals with the existing 2s profile.
+static thread_local std::array<double, 3> g_gpu_phase_ms {};
+#endif
 
 struct DrawIndirectArgs {
 	uint32_t vertex_count_per_instance;
@@ -470,6 +476,21 @@ void GuestGpu::ThreadRun(void* data) {
 	KYTY_PROFILER_THREAD("Thread_Gpu");
 	g_gpu_thread = true;
 	g_gpu_state  = gpu;
+	static const bool frame_profile = [] {
+#if defined(KYTY_B32_FRAME_PROFILE)
+		return true;
+#else
+		const char* value = std::getenv("KYTY_B32_FRAME_PROFILE");
+		return value != nullptr && value[0] == '1' && value[1] == '\0';
+#endif
+	}();
+	using Clock = std::chrono::steady_clock;
+	auto profile_start = Clock::now();
+	double process_ms = 0;
+	uint64_t slices = 0, blocked_slices = 0;
+	std::array<double, 3> type_ms {};
+	std::array<uint64_t, 3> type_slices {};
+	double blocked_ms = 0, max_slice_ms = 0;
 
 	for (;;) {
 		Submission                   submission;
@@ -540,7 +561,47 @@ void GuestGpu::ThreadRun(void* data) {
 		}
 
 		EXIT_IF(!has_submission);
+		const auto slice_start = frame_profile ? Clock::now() : Clock::time_point {};
 		const bool complete = gpu->Process(submission);
+		if (frame_profile) {
+			const auto now = Clock::now();
+			const auto slice_ms = std::chrono::duration<double, std::milli>(now - slice_start).count();
+			process_ms += slice_ms;
+			max_slice_ms = std::max(max_slice_ms, slice_ms);
+			const auto type_index = submission.type == SubmissionType::Graphics ? 0u
+			                      : submission.type == SubmissionType::Compute  ? 1u
+			                                                                    : 2u;
+			type_ms[type_index] += slice_ms;
+			++type_slices[type_index];
+			++slices;
+			blocked_slices += !complete;
+			if (!complete) blocked_ms += slice_ms;
+			const auto window_ms = std::chrono::duration<double, std::milli>(now - profile_start).count();
+			if (window_ms >= 2000.0) {
+				LOGF("Build32 GPU thread profile: window_ms=%.0f process_ms=%.1f slices=%llu "
+				     "blocked_slices=%llu blocked_ms=%.1f graphics_ms=%.1f compute_ms=%.1f "
+				     "flip_ms=%.1f graphics_slices=%llu compute_slices=%llu flip_slices=%llu "
+				     "max_slice_ms=%.1f (CPU wall time; includes shader compilation)\n",
+				     window_ms, process_ms, static_cast<unsigned long long>(slices),
+				     static_cast<unsigned long long>(blocked_slices), blocked_ms,
+				     type_ms[0], type_ms[1], type_ms[2],
+				     static_cast<unsigned long long>(type_slices[0]),
+				     static_cast<unsigned long long>(type_slices[1]),
+				     static_cast<unsigned long long>(type_slices[2]), max_slice_ms);
+#if defined(KYTY_B32_PHASE_PROFILE)
+				LOGF("Build32 GPU phase profile: pm4_ms=%.1f flush_ms=%.1f gc_ms=%.1f "
+				     "(CPU wall time per 2s window)\n",
+				     g_gpu_phase_ms[0], g_gpu_phase_ms[1], g_gpu_phase_ms[2]);
+				g_gpu_phase_ms.fill(0);
+#endif
+				profile_start = now;
+				process_ms = 0;
+				slices = blocked_slices = 0;
+				blocked_ms = max_slice_ms = 0;
+				type_ms.fill(0);
+				type_slices.fill(0);
+			}
+		}
 
 		Common::LockGuard lock(gpu->m_queue_mutex);
 		if (!complete) {
@@ -578,6 +639,37 @@ bool GuestGpu::Process(Submission& submission) {
 
 	cp.BufferInit();
 	bool complete = true;
+	const auto run_pm4 = [&](Pm4Execution& execution, std::span<const uint32_t> commands) {
+#if defined(KYTY_B32_PHASE_PROFILE)
+		const auto start = std::chrono::steady_clock::now();
+#endif
+		const auto result = cp.Process(execution, commands);
+#if defined(KYTY_B32_PHASE_PROFILE)
+		g_gpu_phase_ms[0] += std::chrono::duration<double, std::milli>(
+		    std::chrono::steady_clock::now() - start).count();
+#endif
+		return result;
+	};
+	const auto run_gc = [&] {
+#if defined(KYTY_B32_PHASE_PROFILE)
+		const auto start = std::chrono::steady_clock::now();
+#endif
+		m_renderer.GetGpuResources().RunGarbageCollector();
+#if defined(KYTY_B32_PHASE_PROFILE)
+		g_gpu_phase_ms[2] += std::chrono::duration<double, std::milli>(
+		    std::chrono::steady_clock::now() - start).count();
+#endif
+	};
+	const auto flush = [&] {
+#if defined(KYTY_B32_PHASE_PROFILE)
+		const auto start = std::chrono::steady_clock::now();
+#endif
+		cp.BufferFlush();
+#if defined(KYTY_B32_PHASE_PROFILE)
+		g_gpu_phase_ms[1] += std::chrono::duration<double, std::milli>(
+		    std::chrono::steady_clock::now() - start).count();
+#endif
+	};
 
 	switch (submission.type) {
 		case SubmissionType::Graphics: {
@@ -587,14 +679,14 @@ bool GuestGpu::Process(Submission& submission) {
 				bool round_progress = false;
 				if (!submission.constant_complete) {
 					submission.constant_complete =
-					    cp.Process(submission.constant_execution, submission.constant_commands) ==
+					    run_pm4(submission.constant_execution, submission.constant_commands) ==
 					    Pm4ProcessResult::Complete;
 					round_progress |= submission.constant_execution.MadeProgress();
 				}
 				cp.SetCeComplete(submission.constant_complete);
 				if (!submission.command_complete) {
 					submission.command_complete =
-					    cp.Process(submission.command_execution, submission.commands) ==
+					    run_pm4(submission.command_execution, submission.commands) ==
 					    Pm4ProcessResult::Complete;
 					round_progress |= submission.command_execution.MadeProgress();
 				}
@@ -606,11 +698,11 @@ bool GuestGpu::Process(Submission& submission) {
 			}
 			if (progressed) {
 				if (complete) {
-					m_renderer.GetGpuResources().RunGarbageCollector();
+					run_gc();
 				}
-				cp.BufferFlush();
+				flush();
 			} else if (complete) {
-				m_renderer.GetGpuResources().RunGarbageCollector();
+				run_gc();
 			}
 			break;
 		}
@@ -628,20 +720,20 @@ bool GuestGpu::Process(Submission& submission) {
 			if (first_slice) {
 				GraphicsDbgDumpDcb("cc", num_dw, buffer);
 			}
-			complete = cp.Process(submission.command_execution, submission.commands) ==
+			complete = run_pm4(submission.command_execution, submission.commands) ==
 			           Pm4ProcessResult::Complete;
 			if (submission.command_execution.MadeProgress()) {
 				if (complete) {
-					m_renderer.GetGpuResources().RunGarbageCollector();
+					run_gc();
 				}
-				cp.BufferFlush();
+				flush();
 			} else if (complete) {
-				m_renderer.GetGpuResources().RunGarbageCollector();
+				run_gc();
 			}
 			break;
 		}
 		case SubmissionType::FlipPreparation:
-			m_renderer.GetGpuResources().RunGarbageCollector();
+			run_gc();
 			cp.PrepareCpuFlip(submission.flip_request_id);
 			break;
 	}
@@ -877,10 +969,12 @@ void CommandProcessor::DrawIndex(DrawIndexArgs args) {
 	if (args.instance_count == 0) {
 		args.instance_count = m_num_instances;
 	}
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 	if (args.base_vertex != 0 || args.first_instance != 0) {
 		LOGF("\t draw indexed offsets: base_vertex = %" PRId32 ", first_instance = %" PRIu32 "\n",
 		     args.base_vertex, args.first_instance);
 	}
+#endif
 	m_renderer.GetRenderExecutor().DrawIndex(m_submit_id, CurrentBuffer(), args);
 }
 
diff --git a/src/graphics/host_gpu/renderer/cache/bufferCache.cpp b/src/graphics/host_gpu/renderer/cache/bufferCache.cpp
index f1864a7..308cae4 100644
--- a/src/graphics/host_gpu/renderer/cache/bufferCache.cpp
+++ b/src/graphics/host_gpu/renderer/cache/bufferCache.cpp
@@ -13,6 +13,7 @@
 #include "kernel/memory.h"
 
 #include <algorithm>
+#include <atomic>
 #include <cinttypes>
 #include <cstring>
 #include <memory>
@@ -488,12 +489,26 @@ vk::Buffer BufferCache::UploadCopies(Buffer& buffer, std::span<vk::BufferCopy> c
 	if (copies.empty()) {
 		return nullptr;
 	}
+	const auto read_guest = [](uint64_t address, uint8_t* destination, uint64_t size) {
+		if (Libs::LibKernel::Memory::TryReadBacking(address, destination, size) ||
+		    Libs::LibKernel::Memory::TryReadPrtBacking(address, destination, size)) {
+			return;
+		}
+		// The guest backing may disappear between dirty-range collection and upload.
+		// Do not touch the protected guest pointer from inside the tracker callback.
+		std::memset(destination, 0, static_cast<size_t>(size));
+		static std::atomic<uint32_t> warnings {0};
+		if (warnings.fetch_add(1, std::memory_order_relaxed) < 16) {
+			LOGF("BufferCache: unmapped guest upload at 0x%016" PRIx64
+			     " size=0x%016" PRIx64 "; using zero data\n", address, size);
+		}
+	};
 
 	auto [mapped, base_offset] = m_staging_buffer.Map(total_size, 4);
 	if (mapped != nullptr) {
 		for (auto& copy: copies) {
 			const auto address = buffer.CpuAddress() + copy.dstOffset;
-			std::memcpy(mapped + copy.srcOffset, reinterpret_cast<const void*>(address), copy.size);
+			read_guest(address, mapped + copy.srcOffset, copy.size);
 			copy.srcOffset += base_offset;
 		}
 		m_staging_buffer.Commit();
@@ -504,8 +519,7 @@ vk::Buffer BufferCache::UploadCopies(Buffer& buffer, std::span<vk::BufferCopy> c
 	                                         vk::BufferUsageFlagBits::eTransferSrc, total_size);
 	for (const auto& copy: copies) {
 		const auto address = buffer.CpuAddress() + copy.dstOffset;
-		std::memcpy(temporary->Mapped().data() + copy.srcOffset,
-		            reinterpret_cast<const void*>(address), copy.size);
+		read_guest(address, temporary->Mapped().data() + copy.srcOffset, copy.size);
 	}
 	temporary->Flush(0, total_size);
 	const auto handle = temporary->Handle();
@@ -671,8 +685,9 @@ bool BufferCache::IsRegionCpuModified(uint64_t vaddr, uint64_t size) {
 	return m_memory_tracker.IsRegionCpuModified(vaddr, size);
 }
 
-void BufferCache::RunGarbageCollector() {
-	const auto tick = m_gc_tick++;
+void BufferCache::RunGarbageCollector(uint64_t elapsed_submissions) {
+	const auto tick = m_gc_tick;
+	m_gc_tick += elapsed_submissions;
 	if (m_graphics.CanReportMemoryUsage()) {
 		m_total_used_memory = m_graphics.GetDeviceMemoryUsage();
 	}
diff --git a/src/graphics/host_gpu/renderer/cache/bufferCache.h b/src/graphics/host_gpu/renderer/cache/bufferCache.h
index cc9e511..1268029 100644
--- a/src/graphics/host_gpu/renderer/cache/bufferCache.h
+++ b/src/graphics/host_gpu/renderer/cache/bufferCache.h
@@ -80,7 +80,7 @@ public:
 	[[nodiscard]] uint64_t RegistrationEpoch() const { return m_registration_epoch; }
 	void CollectMappedRegisteredRanges(const RangeSet& mapped, std::vector<RangeSet::Range>& ranges) const;
 
-	void               RunGarbageCollector();
+	void               RunGarbageCollector(uint64_t elapsed_submissions = 1);
 
 private:
 	friend struct BufferCacheTestAccess;
diff --git a/src/graphics/host_gpu/renderer/cache/gpuResourceManager.cpp b/src/graphics/host_gpu/renderer/cache/gpuResourceManager.cpp
index 017042a..1a95c1e 100644
--- a/src/graphics/host_gpu/renderer/cache/gpuResourceManager.cpp
+++ b/src/graphics/host_gpu/renderer/cache/gpuResourceManager.cpp
@@ -176,8 +176,19 @@ void GpuResourceManager::RunGarbageCollector() {
 		m_buffer_cache.ProcessFaultBuffer();
 	}
 	m_texture_cache.ProcessDownloadImages();
+#if defined(KYTY_B32_BATCH_RESOURCE_GC)
+	// A full device-memory query and two cache sweeps per GPU submission are costly.
+	// Keep fault and download handling immediate; sweep at most 16 submissions later.
+	if (++m_gc_submissions < 16) {
+		return;
+	}
+	m_gc_submissions = 0;
+	m_texture_cache.RunGarbageCollector(16);
+	m_buffer_cache.RunGarbageCollector(16);
+#else
 	m_texture_cache.RunGarbageCollector();
 	m_buffer_cache.RunGarbageCollector();
+#endif
 }
 
 } // namespace Libs::Graphics
diff --git a/src/graphics/host_gpu/renderer/cache/gpuResourceManager.h b/src/graphics/host_gpu/renderer/cache/gpuResourceManager.h
index e182166..72956cb 100644
--- a/src/graphics/host_gpu/renderer/cache/gpuResourceManager.h
+++ b/src/graphics/host_gpu/renderer/cache/gpuResourceManager.h
@@ -59,6 +59,7 @@ private:
 	GuestGpu*                 m_gpu = nullptr;
 	std::atomic<uint64_t> m_preparation_alias_epoch {uint64_t{1} << 32};
 	bool                      m_fault_process_pending = false;
+	uint32_t                  m_gc_submissions = 0;
 };
 
 } // namespace Libs::Graphics
diff --git a/src/graphics/host_gpu/renderer/cache/textureCache.cpp b/src/graphics/host_gpu/renderer/cache/textureCache.cpp
index 18d5b4b..463c25d 100644
--- a/src/graphics/host_gpu/renderer/cache/textureCache.cpp
+++ b/src/graphics/host_gpu/renderer/cache/textureCache.cpp
@@ -518,22 +518,26 @@ TextureCache::ImageIds TextureCache::FindImagesInRegion(uint64_t address, uint64
 
 ImageId TextureCache::GetNullImage(const ImageDesc& desc) {
 	const auto format = desc.info.pixel_format;
-	if (const auto found = m_null_images.find(format); found != m_null_images.end()) {
+	const auto key = static_cast<uint64_t>(static_cast<uint32_t>(format)) |
+	                 (static_cast<uint64_t>(static_cast<uint32_t>(desc.info.type)) << 32) |
+	                 (static_cast<uint64_t>(desc.info.samples) << 48);
+	if (const auto found = m_null_images.find(key); found != m_null_images.end()) {
 		return found->second;
 	}
 	ImageInfo info {};
 	info.pixel_format    = desc.info.pixel_format;
 	info.guest_format    = desc.info.guest_format;
-	info.type            = Prospero::ImageType::kColor2D;
+	// Null 3D/1D descriptors still require an image of the matching Vulkan type.
+	info.type            = desc.info.type;
 	info.extent          = {1, 1, 1};
 	info.resources       = {1, 1};
 	info.pitch           = 1;
 	info.bytes_per_block = std::max(desc.info.bytes_per_block, 1u);
-	info.samples         = 1;
+	info.samples         = desc.info.samples;
 	info.tile_mode       = Prospero::TileMode::kLinear;
 	info.mip_layout[0]   = {0, info.bytes_per_block, 1, 1};
 	const auto id        = InsertImage(info);
-	m_null_images.emplace(format, id);
+	m_null_images.emplace(key, id);
 	return id;
 }
 
@@ -2129,9 +2133,10 @@ void TextureCache::UnmapMemory(uint64_t address, uint64_t size) {
 	}
 }
 
-void TextureCache::RunGarbageCollector() {
+void TextureCache::RunGarbageCollector(uint64_t elapsed_submissions) {
 	std::scoped_lock lock {m_lock};
-	const uint64_t   tick = m_gc_tick++;
+	const uint64_t   tick = m_gc_tick;
+	m_gc_tick += elapsed_submissions;
 	if (m_graphics.CanReportMemoryUsage()) {
 		m_total_used_memory = m_graphics.GetDeviceMemoryUsage();
 	}
diff --git a/src/graphics/host_gpu/renderer/cache/textureCache.h b/src/graphics/host_gpu/renderer/cache/textureCache.h
index f373595..c6ffbbc 100644
--- a/src/graphics/host_gpu/renderer/cache/textureCache.h
+++ b/src/graphics/host_gpu/renderer/cache/textureCache.h
@@ -74,7 +74,7 @@ public:
 
 	void UnmapMemory(uint64_t address, uint64_t size);
 	void ProcessDownloadImages();
-	void RunGarbageCollector();
+	void RunGarbageCollector(uint64_t elapsed_submissions = 1);
 
 private:
 	enum class TransferDirection { Upload, Download };
@@ -168,7 +168,7 @@ private:
 	BufferCache&                                      m_buffer_cache;
 	Common::SlotVector<Image>                         m_slot_images;
 	ImagePageTable                                    m_image_page_table;
-	std::unordered_map<vk::Format, ImageId>           m_null_images;
+	std::unordered_map<uint64_t, ImageId>              m_null_images;
 	Common::LeastRecentlyUsedCache<ImageId, uint64_t> m_lru_cache;
 	std::unordered_set<ImageId>                       m_download_images;
 	std::map<uint64_t, MetaDataInfo>                  m_surface_metas;
diff --git a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp
index 83cabcd..4b513e6 100644
--- a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp
+++ b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.cpp
@@ -100,8 +100,23 @@ void PipelineCacheLog(fmt::format_string<Args...> format, Args&&... args) {
 }
 
 bool ReadShaderGuestMemory(void*, uint64_t address, uint32_t* value) {
-	return value != nullptr &&
-	       Libs::LibKernel::Memory::TryReadGpuCleanBacking(address, value, sizeof(*value));
+	if (value == nullptr) {
+		return false;
+	}
+	if (Libs::LibKernel::Memory::TryReadGpuCleanBacking(address, value, sizeof(*value))) {
+		return true;
+	}
+	// After a reload a descriptor may still belong to a GPU-written buffer.
+	// Drain that owner before failing the clean read; never read stale backing.
+	if (Libs::LibKernel::Memory::SyncGpuCleanBacking(address, sizeof(*value)) &&
+	    Libs::LibKernel::Memory::TryReadGpuCleanBacking(address, value, sizeof(*value))) {
+		return true;
+	}
+	static std::atomic_uint rejected_reads {0};
+	if (rejected_reads.fetch_add(1, std::memory_order_relaxed) < 8) {
+		PipelineCacheLog("Shader clean descriptor read unavailable at 0x{:016x}", address);
+	}
+	return false;
 }
 
 bool SyncShaderGuestMemory(void*, uint64_t address, uint64_t size) {
@@ -119,8 +134,15 @@ bool ReadShaderRawGuestMemory(void*, uint64_t address, uint32_t* value) {
 }
 
 bool ReadShaderMemorySpan(void*, uint64_t address, uint32_t* values, uint32_t count, bool clean) {
-	return count >= 2 && count <= 16 &&
-	       Libs::LibKernel::Memory::TryReadGpuShaderSpan(address, values, count * 4u, clean);
+	if (count < 2 || count > 16) {
+		return false;
+	}
+	const auto size = count * 4u;
+	if (Libs::LibKernel::Memory::TryReadGpuShaderSpan(address, values, size, clean)) {
+		return true;
+	}
+	return clean && Libs::LibKernel::Memory::SyncGpuCleanBacking(address, size) &&
+	       Libs::LibKernel::Memory::TryReadGpuShaderSpan(address, values, size, true);
 }
 
 void ReportMaterialization(const char* label, ShaderType stage, uint64_t hash,
@@ -279,8 +301,15 @@ struct PipelineCache::ProgramCache {
 			case ShaderType::Compute: stage_name = "cs"; break;
 			default: EXIT("invalid pipeline shader stage\n");
 		}
+		const auto compile_start = std::chrono::steady_clock::now();
 		auto result = ShaderRecompiler::CompileProgram(std::move(translated), options,
 		                                               specialization, push_data_start_dword);
+		const auto compile_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
+		    std::chrono::steady_clock::now() - compile_start).count();
+		if (compile_ms >= 16) {
+			PipelineCacheLog("Build32 stall: shader compile stage={} hash=0x{:016x} ms={}",
+			                 stage_name, options.shader_hash, compile_ms);
+		}
 		DumpShaderOriginal(stage_name, options.shader_hash, params.code, result.decoded_dump);
 		if (!ValidateShaderSpirv(options.dump_label, options.shader_hash, result.spirv)) {
 			DumpShaderSpirv(stage_name, options.shader_hash, result.spirv);
@@ -390,6 +419,18 @@ struct PipelineCache::ProgramCache {
 		options.back_code      = params.back_code;
 		options.dump_ir     = Config::GetShaderLogDirection() != Config::ShaderLogDirection::Silent;
 		options.early_dump  = options.dump_ir;
+#if defined(KYTY_B32_FORCE_SILENT_SHADER_DUMPS)
+		// Keep this performance test comparable even if a per-game launcher setting
+		// requests verbose shader IR. Report the effective runtime setting once.
+		static const bool logged_shader_setting = [] {
+			LOGF("Build32 shader diagnostics: requested=%u forced=Silent (performance test)\n",
+			     static_cast<unsigned>(Config::GetShaderLogDirection()));
+			return true;
+		}();
+		(void)logged_shader_setting;
+		options.dump_ir    = false;
+		options.early_dump = false;
+#endif
 		options.dump_label  = label;
 		options.input_info  = stage_input;
 		options.scratch_dwords = input_info.scratch_size_dwords;
@@ -403,7 +444,14 @@ struct PipelineCache::ProgramCache {
 		} else if constexpr (std::is_same_v<InputInfo, ShaderComputeInputInfo>) {
 			options.wave_size = input_info.wave_size;
 		}
+		const auto translate_start = std::chrono::steady_clock::now();
 		auto translated = ShaderRecompiler::TranslateProgram(params.code, options);
+		const auto translate_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
+		    std::chrono::steady_clock::now() - translate_start).count();
+		if (translate_ms >= 16) {
+			PipelineCacheLog("Build32 stall: shader translate stage={} hash=0x{:016x} ms={}",
+			                 static_cast<uint32_t>(stage), params.hash, translate_ms);
+		}
 		if (entry == programs.end()) {
 			auto resource_plan = ShaderRecompiler::IR::ExtractResourcePlan(translated.program);
 			ReportMaterialization(label, stage, params.hash, report,
@@ -486,11 +534,6 @@ void PipelineCache::InitializeDriverCache() {
 		PipelineCacheLog("Vulkan pipeline cache: disabled (unknown git revision)");
 		return;
 	}
-	if (git_hash.ends_with("-dirty")) {
-		PipelineCacheLog("Vulkan pipeline cache: disabled (dirty build)");
-		return;
-	}
-
 	m_driver_cache_path     = std::filesystem::path("_PipelineCache") / (title_id + ".bin");
 	const auto path         = Common::PathToString(m_driver_cache_path);
 	const bool cache_exists = Common::File::IsFileExisting(m_driver_cache_path);
@@ -559,6 +602,30 @@ void PipelineCache::InitializeDriverCache() {
 
 void PipelineCache::Save() {
 	Common::LockGuard lock(m_mutex);
+	SaveDriverCacheLocked(true);
+}
+
+void PipelineCache::CheckpointDriverCacheLocked() {
+	if (m_driver_cache == nullptr || ++m_new_pipelines_since_checkpoint < 16) {
+		return;
+	}
+	const auto now = std::chrono::steady_clock::now();
+	if (now - m_last_driver_cache_checkpoint < std::chrono::seconds(90)) {
+		return;
+	}
+	// A cache snapshot can stall the renderer. Limit snapshots to active periods
+	// with new pipelines and leave the in-memory cache available for this session.
+	m_new_pipelines_since_checkpoint = 0;
+	m_last_driver_cache_checkpoint = now;
+	SaveDriverCacheLocked(false);
+	const auto checkpoint_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
+	    std::chrono::steady_clock::now() - now).count();
+	if (checkpoint_ms >= 16) {
+		PipelineCacheLog("Build32 stall: cache checkpoint ms={}", checkpoint_ms);
+	}
+}
+
+void PipelineCache::SaveDriverCacheLocked(bool final) {
 	if (m_driver_cache == nullptr) {
 		return;
 	}
@@ -612,8 +679,10 @@ void PipelineCache::Save() {
 	}
 	PipelineCacheLog("Vulkan pipeline cache: saved {} bytes to {}", payload.size(),
 	                 Common::PathToString(m_driver_cache_path));
-	m_graphics.device.destroyPipelineCache(m_driver_cache, nullptr);
-	m_driver_cache = nullptr;
+	if (final) {
+		m_graphics.device.destroyPipelineCache(m_driver_cache, nullptr);
+		m_driver_cache = nullptr;
+	}
 }
 
 PipelineCache::GraphicsPrograms PipelineCache::GetGraphicsPrograms(
@@ -840,9 +909,16 @@ PipelineCache::Pipeline& PipelineCache::CreateGraphicsPipeline(
 
 	auto cached = std::make_unique<Pipeline>();
 	LogPipelineTrace("CreatePipelineInternal begin", vs_id, ps_id);
+	const auto pipeline_start = std::chrono::steady_clock::now();
 	CreatePipelineInternal(m_graphics, *cached, rendering, key.vertex_input, vs_input_info,
 	                       vertex_program, ps_input_info, pixel_program, static_params,
 	                       m_driver_cache);
+	const auto pipeline_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
+	    std::chrono::steady_clock::now() - pipeline_start).count();
+	if (pipeline_ms >= 16) {
+		PipelineCacheLog("Build32 stall: graphics pipeline vs={} ps={} ms={}",
+		                 vs_id, ps_id, pipeline_ms);
+	}
 	LogPipelineTrace("CreatePipelineInternal done", vs_id, ps_id);
 
 	EXIT_NOT_IMPLEMENTED(cached->pipeline == nullptr);
@@ -850,6 +926,7 @@ PipelineCache::Pipeline& PipelineCache::CreateGraphicsPipeline(
 
 	auto [iter, inserted] = m_graphics_pipelines.emplace(std::move(key), std::move(cached));
 	EXIT_IF(!inserted);
+	CheckpointDriverCacheLocked();
 
 	return *iter->second;
 }
@@ -873,13 +950,21 @@ PipelineCache::CreateComputePipeline(const ShaderComputeInputInfo& input_info,
 	}
 
 	auto cached = std::make_unique<Pipeline>();
+	const auto pipeline_start = std::chrono::steady_clock::now();
 	CreatePipelineInternal(m_graphics, *cached, input_info, compute_program.module, m_driver_cache);
+	const auto pipeline_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
+	    std::chrono::steady_clock::now() - pipeline_start).count();
+	if (pipeline_ms >= 16) {
+		PipelineCacheLog("Build32 stall: compute pipeline shader={} ms={}",
+		                 compute_program.id, pipeline_ms);
+	}
 
 	EXIT_NOT_IMPLEMENTED(cached->pipeline == nullptr);
 	EXIT_NOT_IMPLEMENTED(cached->pipeline_layout == nullptr);
 
 	auto [iter, inserted] = m_compute_pipelines.emplace(compute_program.id, std::move(cached));
 	EXIT_IF(!inserted);
+	CheckpointDriverCacheLocked();
 
 	return *iter->second;
 }
diff --git a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h
index 6fa19e7..e517c2e 100644
--- a/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h
+++ b/src/graphics/host_gpu/renderer/pipeline/pipelineCache.h
@@ -9,6 +9,7 @@
 #include "graphics/host_gpu/vulkanCommon.h"
 #include "graphics/shader/shader.h"
 
+#include <chrono>
 #include <cstddef>
 #include <filesystem>
 #include <memory>
@@ -211,12 +212,17 @@ private:
 	std::unique_ptr<ProgramCache> m_program_cache;
 	vk::PipelineCache             m_driver_cache = nullptr;
 	std::filesystem::path         m_driver_cache_path;
+	uint32_t                      m_new_pipelines_since_checkpoint = 0;
+	std::chrono::steady_clock::time_point m_last_driver_cache_checkpoint =
+	    std::chrono::steady_clock::now();
 	std::unordered_map<GraphicsPipelineKey, std::unique_ptr<Pipeline>, GraphicsPipelineKeyHash>
 	                                                        m_graphics_pipelines;
 	std::unordered_map<uint64_t, std::unique_ptr<Pipeline>> m_compute_pipelines;
 	Common::Mutex m_mutex;
 
 	void InitializeDriverCache();
+	void SaveDriverCacheLocked(bool final);
+	void CheckpointDriverCacheLocked();
 };
 
 void LogPipelineTrace(const char* phase, uint64_t vertex_program_id, uint64_t pixel_program_id);
diff --git a/src/graphics/host_gpu/renderer/sync.cpp b/src/graphics/host_gpu/renderer/sync.cpp
index db3003c..c5669fb 100644
--- a/src/graphics/host_gpu/renderer/sync.cpp
+++ b/src/graphics/host_gpu/renderer/sync.cpp
@@ -117,9 +117,11 @@ void WriteAtEndOfPipeClockCounter(uint64_t submit_id, CommandBuffer& buffer, uin
 	RecordEndOfPipeWrite(submit_id, buffer, reinterpret_cast<uint64_t>(dst_gpu_addr), 0,
 	                     EndOfPipeWriteSize::Qword, EndOfPipeWriteAction::Write);
 
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 	LOGF_COLOR(Log::Color::BrightGreen,
 	           "EndOfPipe Signal!!! [0x%016" PRIx64 "] <- Clock: 0x%016" PRIx64 "\n",
 	           reinterpret_cast<uint64_t>(dst_gpu_addr), value);
+#endif
 }
 
 void WriteAtEndOfPipeClockCounterWithWriteBack(uint64_t submit_id, CommandBuffer& buffer,
@@ -127,9 +129,11 @@ void WriteAtEndOfPipeClockCounterWithWriteBack(uint64_t submit_id, CommandBuffer
 	RecordEndOfPipeWrite(submit_id, buffer, reinterpret_cast<uint64_t>(dst_gpu_addr), 0,
 	                     EndOfPipeWriteSize::Qword, EndOfPipeWriteAction::WriteBack);
 
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 	LOGF_COLOR(Log::Color::BrightGreen,
 	           "EndOfPipe Signal!!! [0x%016" PRIx64 "] <- Clock: 0x%016" PRIx64 "\n",
 	           reinterpret_cast<uint64_t>(dst_gpu_addr), value);
+#endif
 }
 
 void WriteAtEndOfPipeWithWriteBack64(uint64_t submit_id, CommandBuffer& buffer,
diff --git a/src/graphics/host_gpu/vma.cpp b/src/graphics/host_gpu/vma.cpp
index f7b512f..3deb1fa 100644
--- a/src/graphics/host_gpu/vma.cpp
+++ b/src/graphics/host_gpu/vma.cpp
@@ -136,11 +136,29 @@ bool GraphicContext::CreateImage(const vk::ImageCreateInfo& image_info, VulkanIm
 	alloc_info.requiredFlags = VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT;
 
 	vk::Image::CType native_image = VK_NULL_HANDLE;
-	const auto        result       = static_cast<vk::Result>(
+	auto              result       = static_cast<vk::Result>(
 	    vmaCreateImage(allocator, static_cast<const vk::ImageCreateInfo::NativeType*>(image_info),
 	                   &alloc_info, &native_image, &image.allocation, nullptr));
+	if (result == vk::Result::eErrorOutOfDeviceMemory ||
+	    result == vk::Result::eErrorOutOfHostMemory) {
+		// Prefer the original device-local allocation. If it is exhausted, let VMA
+		// choose another memory type supported by this image before giving up.
+		LOGF("vmaCreateImage: %s; retrying with device-local memory preferred\n",
+		     vk::to_string(result).c_str());
+		native_image        = VK_NULL_HANDLE;
+		image.allocation    = nullptr;
+		alloc_info.requiredFlags  = 0;
+		alloc_info.preferredFlags = VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT;
+		result = static_cast<vk::Result>(vmaCreateImage(
+		    allocator, static_cast<const vk::ImageCreateInfo::NativeType*>(image_info),
+		    &alloc_info, &native_image, &image.allocation, nullptr));
+	}
 	image.image = native_image;
 	if (result != vk::Result::eSuccess) {
+		LOGF("vmaCreateImage failed: %s, format=%d extent=%ux%ux%u layers=%u levels=%u\n",
+		     vk::to_string(result).c_str(), static_cast<int>(image_info.format),
+		     image_info.extent.width, image_info.extent.height, image_info.extent.depth,
+		     image_info.arrayLayers, image_info.mipLevels);
 		LogMemoryBudget();
 		return false;
 	}
diff --git a/src/graphics/presentation/window/swapchain.cpp b/src/graphics/presentation/window/swapchain.cpp
index 166d772..87e2be9 100644
--- a/src/graphics/presentation/window/swapchain.cpp
+++ b/src/graphics/presentation/window/swapchain.cpp
@@ -14,6 +14,8 @@
 #include "graphics/presentation/window/windowInternal.h"
 
 #include <algorithm>
+#include <chrono>
+#include <cstdlib>
 #include <deque>
 #include <limits>
 #include <memory>
@@ -765,12 +767,28 @@ RenderContext& Presenter::Renderer() const noexcept {
 
 void Presenter::Present(Frame& frame, bool reuse) {
 	KYTY_PROFILER_FUNCTION();
+	// CPU-side frame timings; the Vulkan work may still be running after Present returns.
+	static const bool frame_profile = [] {
+#if defined(KYTY_B32_FRAME_PROFILE)
+		return true;
+#else
+		const char* value = std::getenv("KYTY_B32_FRAME_PROFILE");
+		return value != nullptr && value[0] == '1' && value[1] == '\0';
+#endif
+	}();
+	using Clock = std::chrono::steady_clock;
+	static Clock::time_point last_frame;
+	static uint64_t frames = 0;
+	static uint64_t reused_frames = 0;
+	static double interval_ms = 0, acquire_ms = 0, record_ms = 0, present_ms = 0;
+	const auto start = frame_profile ? Clock::now() : Clock::time_point {};
 	m_impl->frames.ValidateForPresent(&frame, reuse);
 
 	const auto overlay_visual = GetSystemOverlayVisualState();
 	auto&      swapchain  = m_impl->swapchain;
 	for (uint32_t attempt = 0; attempt < 2; attempt++) {
 		auto status = swapchain.AcquireNextImage();
+		const auto acquired = frame_profile ? Clock::now() : Clock::time_point {};
 		if (status != Swapchain::Status::Success) {
 			m_impl->RecoverSwapchain(status);
 			continue;
@@ -783,7 +801,9 @@ void Presenter::Present(Frame& frame, bool reuse) {
 			swapchain.RecordPresentCommands(command, frame.image, draw_system_overlay);
 			frame.present_tick = swapchain.Submit(m_impl->present_scheduler);
 		}
+		const auto submitted = frame_profile ? Clock::now() : Clock::time_point {};
 		status = swapchain.Present();
+		const auto presented = frame_profile ? Clock::now() : Clock::time_point {};
 		if (status != Swapchain::Status::Success) {
 			m_impl->RecoverSwapchain(status);
 			continue;
@@ -793,6 +813,29 @@ void Presenter::Present(Frame& frame, bool reuse) {
 		                                         std::memory_order_release);
 		m_impl->window.UpdateTitle();
 		m_impl->frames.Release(&frame, true);
+		if (frame_profile) {
+			const auto elapsed = [](Clock::time_point a, Clock::time_point b) {
+				return std::chrono::duration<double, std::milli>(b - a).count();
+			};
+			if (last_frame != Clock::time_point {}) {
+				interval_ms += elapsed(last_frame, start);
+				acquire_ms += elapsed(start, acquired);
+				record_ms += elapsed(acquired, submitted);
+				present_ms += elapsed(submitted, presented);
+				reused_frames += reuse;
+				if (++frames == 120) {
+					LOGF("Build32 frame profile: frames=%llu reused=%llu interval_ms=%.2f acquire_ms=%.2f "
+					     "record_submit_ms=%.2f present_ms=%.2f (CPU wall time averages)\n",
+					     static_cast<unsigned long long>(frames),
+					     static_cast<unsigned long long>(reused_frames), interval_ms / frames,
+					     acquire_ms / frames, record_ms / frames, present_ms / frames);
+					frames = 0;
+					reused_frames = 0;
+					interval_ms = acquire_ms = record_ms = present_ms = 0;
+				}
+			}
+			last_frame = start;
+		}
 		return;
 	}
 	LOGF("Vulkan presentation retry exhausted; dropping frame\n");
diff --git a/src/graphics/shader/recompiler/ShaderRecompiler.cpp b/src/graphics/shader/recompiler/ShaderRecompiler.cpp
index 4aa50e0..4c7bb07 100644
--- a/src/graphics/shader/recompiler/ShaderRecompiler.cpp
+++ b/src/graphics/shader/recompiler/ShaderRecompiler.cpp
@@ -21,8 +21,10 @@
 #include <algorithm>
 #include <array>
 #include <chrono>
+#include <deque>
 #include <fmt/format.h>
 #include <map>
+#include <mutex>
 #include <span>
 #include <utility>
 
@@ -502,6 +504,70 @@ Decoder::Program DecodeFusedProgram(std::span<const uint32_t> front, std::span<c
 
 } // namespace
 
+namespace {
+// The decoded instructions and CFG depend on guest code, not on the current
+// descriptor contents. Bound the cache to avoid retaining every game shader.
+struct CachedComputeFrontend {
+	uint64_t shader_hash = 0;
+	std::vector<uint32_t> code;
+	std::vector<Decoder::Instruction> instructions;
+	CFG::Graph graph;
+	bool dispatcher_fallback = false;
+	std::string dispatcher_reason;
+	size_t estimated_bytes = 0;
+};
+
+std::mutex g_compute_frontend_mutex;
+std::deque<CachedComputeFrontend> g_compute_frontends;
+constexpr size_t MaxCachedComputeFrontends = 64;
+constexpr size_t MaxCachedComputeFrontendBytes = 96ull * 1024 * 1024;
+size_t g_compute_frontend_bytes = 0;
+
+bool FindCachedComputeFrontend(std::span<const uint32_t> code, uint64_t hash,
+                               Decoder::Program& decoded, CFG::Graph& graph,
+                               bool& dispatcher_fallback, std::string& dispatcher_reason) {
+	std::lock_guard lock(g_compute_frontend_mutex);
+	for (auto& entry: g_compute_frontends) {
+		if (entry.shader_hash != hash || entry.code.size() != code.size() ||
+		    !std::equal(entry.code.begin(), entry.code.end(), code.begin())) {
+			continue;
+		}
+		decoded.code = code;
+		decoded.instructions = entry.instructions;
+		graph = entry.graph;
+		dispatcher_fallback = entry.dispatcher_fallback;
+		dispatcher_reason = entry.dispatcher_reason;
+		return true;
+	}
+	return false;
+}
+
+void RememberComputeFrontend(std::span<const uint32_t> code, uint64_t hash,
+                             const Decoder::Program& decoded, const CFG::Graph& graph,
+                             bool dispatcher_fallback, const std::string& dispatcher_reason) {
+	if (code.size() > 16384) {
+		return;
+	}
+	// Conservatively account for nested CFG vectors as well as instruction storage.
+	const size_t estimated_bytes = code.size() * sizeof(uint32_t) +
+	    decoded.instructions.size() * sizeof(Decoder::Instruction) +
+	    graph.blocks.size() * sizeof(CFG::BasicBlock) * 4;
+	if (estimated_bytes > MaxCachedComputeFrontendBytes) {
+		return;
+	}
+	std::lock_guard lock(g_compute_frontend_mutex);
+	while (!g_compute_frontends.empty() &&
+	       (g_compute_frontends.size() >= MaxCachedComputeFrontends ||
+	        g_compute_frontend_bytes > MaxCachedComputeFrontendBytes - estimated_bytes)) {
+		g_compute_frontend_bytes -= g_compute_frontends.front().estimated_bytes;
+		g_compute_frontends.pop_front();
+	}
+	g_compute_frontends.push_back({hash, {code.begin(), code.end()}, decoded.instructions,
+	                               graph, dispatcher_fallback, dispatcher_reason, estimated_bytes});
+	g_compute_frontend_bytes += estimated_bytes;
+}
+} // namespace
+
 TranslateResult TranslateProgram(std::span<const uint32_t> code, const CompileOptions& options) {
 	if (code.empty()) {
 		EXIT("shader recompiler input is empty\n");
@@ -524,11 +590,19 @@ TranslateResult TranslateProgram(std::span<const uint32_t> code, const CompileOp
 	     static_cast<uint64_t>(code.size()));
 
 	Decoder::Program decoded;
+	CFG::Graph cfg;
+	bool dispatcher_fallback = false;
+	std::string dispatcher_reason;
+	const bool cache_eligible = options.stage == ShaderType::Compute && options.back_code.empty();
+	const bool frontend_hit = cache_eligible && FindCachedComputeFrontend(
+	    code, options.shader_hash, decoded, cfg, dispatcher_fallback, dispatcher_reason);
 	std::vector<uint32_t> joined_code;
-	if (!options.back_code.empty()) {
-		decoded = DecodeFusedProgram(code, options.back_code, joined_code);
-	} else {
-		Decoder::DecodeProgram(code, decoded);
+	if (!frontend_hit) {
+		if (!options.back_code.empty()) {
+			decoded = DecodeFusedProgram(code, options.back_code, joined_code);
+		} else {
+			Decoder::DecodeProgram(code, decoded);
+		}
 	}
 	LOGF("%s phase end: stage=%s hash=0x%016" PRIx64 " decode instructions=%" PRIu64
 	     " elapsed_ms=%" PRIu64 "\n",
@@ -545,15 +619,16 @@ TranslateResult TranslateProgram(std::span<const uint32_t> code, const CompileOp
 
 	LOGF("%s phase begin: stage=%s hash=0x%016" PRIx64 " CFG BuildGraph\n", GetDumpLabel(options),
 	     StageName(options.stage), options.shader_hash);
-	auto cfg = CFG::BuildGraph(decoded);
+	if (!frontend_hit) cfg = CFG::BuildGraph(decoded);
 	LOGF("%s phase end: stage=%s hash=0x%016" PRIx64 " CFG BuildGraph blocks=%" PRIu64
 	     " loops=%" PRIu64 " back_edges=%" PRIu64 " elapsed_ms=%" PRIu64 "\n",
 	     GetDumpLabel(options), StageName(options.stage), options.shader_hash,
 	     static_cast<uint64_t>(cfg.blocks.size()), static_cast<uint64_t>(cfg.natural_loops.size()),
 	     static_cast<uint64_t>(cfg.back_edges.size()), phase_ms());
-	bool        dispatcher_fallback = false;
-	std::string dispatcher_reason;
-	if (cfg.irreducible) {
+	if (frontend_hit) {
+		// Reuse the previously structured graph; later resource processing still
+		// runs with this invocation's inputs.
+	} else if (cfg.irreducible) {
 		dispatcher_fallback = true;
 		dispatcher_reason   = cfg.unsupported_reason;
 		LogDispatcherFallback(options, cfg, "build", dispatcher_reason);
@@ -630,11 +705,21 @@ TranslateResult TranslateProgram(std::span<const uint32_t> code, const CompileOp
 	     " elapsed_ms=%" PRIu64 "\n",
 	     GetDumpLabel(options), StageName(options.stage), options.shader_hash,
 	     static_cast<uint64_t>(ir.blocks.size()), phase_ms());
+	const auto slow_pass = [&](const char* name, uint64_t begin_ms) {
+		const auto elapsed = phase_ms() - begin_ms;
+		if (options.stage == ShaderType::Compute && elapsed >= 16) {
+			LOGF("%s slow IR pass: hash=0x%016" PRIx64 " name=%s elapsed_ms=%" PRIu64 "\n",
+			     GetDumpLabel(options), options.shader_hash, name, elapsed);
+		}
+	};
+	auto pass_start = phase_ms();
 	IR::RewriteToSsa(ir.blocks);
 	IR::ConstantPropagationPass(ir.blocks);
 	IR::ResolveControlFlowIdentities(ir);
 	IR::RemoveIdentities(ir.blocks);
 	IR::EliminateDeadCode(ir.blocks);
+	slow_pass("ssa+basic-opts", pass_start);
+	pass_start = phase_ms();
 	const auto read_lane_stats = IR::EliminateReadLane(ir, ir.wave_size);
 	if (read_lane_stats.rewritten_reads != 0) {
 		LOGF("%s read-lane elimination: reads=%" PRIu32 "\n", GetDumpLabel(options),
@@ -644,6 +729,8 @@ TranslateResult TranslateProgram(std::span<const uint32_t> code, const CompileOp
 		IR::RemoveIdentities(ir.blocks);
 		IR::EliminateDeadCode(ir.blocks);
 	}
+	slow_pass("read-lane", pass_start);
+	pass_start = phase_ms();
 	const auto waterfalls = IR::RewriteWaterfallDescriptors(ir);
 	if (waterfalls != 0) {
 		LOGF("%s waterfall descriptor de-scalarization: stage=%s hash=0x%016" PRIx64
@@ -654,14 +741,23 @@ TranslateResult TranslateProgram(std::span<const uint32_t> code, const CompileOp
 		IR::RemoveIdentities(ir.blocks);
 		IR::EliminateDeadCode(ir.blocks);
 	}
+	slow_pass("waterfall", pass_start);
+	pass_start = phase_ms();
 	IR::BuildSrtPlan(ir);
 	IR::EliminateDeadCode(ir.blocks);
+	slow_pass("srt-plan", pass_start);
 	if (options.dump_ir && options.early_dump) {
 		LOGF("%s native IR before resource tracking:\n%s", GetDumpLabel(options),
 		     IR::ProgramToString(ir).c_str());
 	}
+	pass_start = phase_ms();
 	IR::TrackResources(ir);
 	IR::EliminateDeadCode(ir.blocks);
+	slow_pass("resource-tracking", pass_start);
+	if (cache_eligible && !frontend_hit && phase_ms() >= 16) {
+		RememberComputeFrontend(code, options.shader_hash, decoded, cfg,
+		                        dispatcher_fallback, dispatcher_reason);
+	}
 	TranslateResult result;
 	result.program = std::move(ir);
 	if (options.dump_ir) {
diff --git a/src/graphics/shader/recompiler/frontend/cfg/ShaderCFG.cpp b/src/graphics/shader/recompiler/frontend/cfg/ShaderCFG.cpp
index c00421d..44e19a8 100644
--- a/src/graphics/shader/recompiler/frontend/cfg/ShaderCFG.cpp
+++ b/src/graphics/shader/recompiler/frontend/cfg/ShaderCFG.cpp
@@ -3,6 +3,7 @@
 #include "common/assert.h"
 
 #include <algorithm>
+#include <bit>
 #include <fmt/format.h>
 #include <iterator>
 #include <map>
@@ -719,8 +720,70 @@ void PruneUnreachableBlocks(Graph& graph) {
 	RebuildPredecessors(graph);
 }
 
+// Repeated CFG rewrites recompute dominance many times. Large graphs use
+// compact bitsets for the fixed-point intersections, then publish the same
+// sorted block-id vectors expected by the rest of the structurizer.
+void ComputeLargeDominatorSets(Graph& graph, bool post) {
+	const auto count = static_cast<uint32_t>(graph.blocks.size());
+	const auto words = (count + 63u) / 64u;
+	std::vector<uint64_t> all(words, UINT64_MAX);
+	if (count % 64u != 0) {
+		all.back() = (uint64_t {1} << (count % 64u)) - 1u;
+	}
+	std::vector<std::vector<uint64_t>> sets(count, all);
+	for (const auto& block: graph.blocks) {
+		const bool root = post ? block.successors.empty() : block.id == graph.entry_block;
+		if (root) {
+			std::fill(sets[block.id].begin(), sets[block.id].end(), 0);
+			sets[block.id][block.id / 64u] |= uint64_t {1} << (block.id % 64u);
+		}
+	}
+	std::vector<uint64_t> next(words);
+	bool changed = true;
+	while (changed) {
+		changed = false;
+		for (const auto& block: graph.blocks) {
+			if (!post && block.id == graph.entry_block) {
+				continue;
+			}
+			const auto& neighbours = post ? block.successors : block.predecessors;
+			if (neighbours.empty()) {
+				std::fill(next.begin(), next.end(), 0);
+			} else {
+				std::copy(sets[neighbours.front()].begin(), sets[neighbours.front()].end(),
+				          next.begin());
+				for (size_t i = 1; i < neighbours.size(); ++i) {
+					for (size_t word = 0; word < words; ++word) {
+						next[word] &= sets[neighbours[i]][word];
+					}
+				}
+			}
+			next[block.id / 64u] |= uint64_t {1} << (block.id % 64u);
+			if (next != sets[block.id]) {
+				std::copy(next.begin(), next.end(), sets[block.id].begin());
+				changed = true;
+			}
+		}
+	}
+	for (auto& block: graph.blocks) {
+		auto& ids = post ? block.post_dominators : block.dominators;
+		ids.clear();
+		for (size_t word = 0; word < words; ++word) {
+			auto bits = sets[block.id][word];
+			while (bits != 0) {
+				ids.push_back(static_cast<uint32_t>(word * 64u + std::countr_zero(bits)));
+				bits &= bits - 1u;
+			}
+		}
+	}
+}
+
 void ComputeDominators(Graph& graph) {
 	const auto count = static_cast<uint32_t>(graph.blocks.size());
+	if (count >= 128) {
+		ComputeLargeDominatorSets(graph, false);
+		return;
+	}
 	const auto all   = AllBlockIds(count);
 
 	for (auto& block: graph.blocks) {
@@ -755,6 +818,10 @@ void ComputeDominators(Graph& graph) {
 
 void ComputePostDominators(Graph& graph) {
 	const auto count = static_cast<uint32_t>(graph.blocks.size());
+	if (count >= 128) {
+		ComputeLargeDominatorSets(graph, true);
+		return;
+	}
 	const auto all   = AllBlockIds(count);
 
 	for (auto& block: graph.blocks) {
diff --git a/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp b/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp
index bf1c28c..d398b24 100644
--- a/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp
+++ b/src/graphics/shader/recompiler/ir/passes/ResourceMaterialization.cpp
@@ -1,11 +1,13 @@
 #include "graphics/shader/recompiler/ir/passes/ResourceMaterialization.h"
 
 #include "common/assert.h"
+#include "common/logging/log.h"
 #include "graphics/guest_gpu/gpu_format.h"
 #include "graphics/shader/recompiler/ir/ShaderIR.h"
 #include "graphics/shader/shaderBindings.h"
 
 #include <algorithm>
+#include <atomic>
 #include <array>
 #include <bit>
 #include <cstdio>
@@ -622,6 +624,19 @@ static bool MaterializeSnapshot(const ResourcePlan& program, const SrtRuntime& r
 			clean_runtime.read_memory = runtime.read_specialization_memory;
 			std::vector<DescriptorValue> tables;
 			if (!EvaluateDescriptorSources(program, requests, clean_runtime, tables)) {
+				if (program.stage == ShaderType::Compute &&
+				    program.shader_hash == 0x05021f35c669b111ull) {
+					// During reload this table can be unavailable while the guest rebuilds
+					// its heap. Match the inactive-source null-image path for this shader;
+					// the next dispatch will read the table again.
+					static std::atomic_bool logged_unavailable_table {false};
+					if (!logged_unavailable_table.exchange(true, std::memory_order_relaxed)) {
+						LOGF("SRT indirect image table unavailable: shader=0x%016llx image=%u; using null image\n",
+						     static_cast<unsigned long long>(program.shader_hash), image_index);
+					}
+					next.images[image_index].dword_count = 8u;
+				continue;
+				}
 				return fail("an indirect image table source did not evaluate");
 			}
 			IndirectImage table;
diff --git a/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp b/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp
index 2aedc25..6a24d1b 100644
--- a/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp
+++ b/src/graphics/shader/recompiler/ir/passes/SrtWalker.cpp
@@ -1,11 +1,18 @@
+#ifndef NOMINMAX
+#define NOMINMAX
+#endif
+
 #include "graphics/shader/recompiler/ir/passes/LinearSrt.h"
 #include "graphics/shader/recompiler/ir/passes/SrtWalker.h"
 
 #include "common/assert.h"
+#include "common/logging/log.h"
 #include "graphics/shader/recompiler/ir/ShaderIR.h"
 
 #include <algorithm>
+#include <atomic>
 #include <cstdlib>
+#include <cstdio>
 #include <cstring>
 #include <bit>
 #include <cmath>
@@ -1173,10 +1180,14 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
                                 std::span<const uint8_t> clean_flat_slots,
                                 std::vector<uint8_t>& active_sources) {
 	if (!program.srt_plan_complete) {
+		LOGF("SRT evaluation missing plan: shader=0x%016llx\n",
+		     static_cast<unsigned long long>(program.shader_hash));
 		return false;
 	}
 	if (std::ranges::any_of(clean_flat_slots, [](uint8_t clean) { return clean != 0u; }) &&
 	    runtime.read_specialization_memory == nullptr) {
+		LOGF("SRT evaluation missing specialization reader: shader=0x%016llx\n",
+		     static_cast<unsigned long long>(program.shader_hash));
 		return false;
 	}
 	SrtRuntime clean_runtime  = runtime;
@@ -1190,6 +1201,8 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
 	for (const auto source_index: sources) {
 		const auto* source = Source(program, source_index);
 		if (source == nullptr) {
+			LOGF("SRT evaluation missing source: shader=0x%016llx source=%u\n",
+			     static_cast<unsigned long long>(program.shader_hash), source_index);
 			return false;
 		}
 		DescriptorValue value;
@@ -1197,7 +1210,31 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
 		if (!evaluate_flat || active[source_index]) {
 			for (uint32_t index = 0; index < source->dword_count; index++) {
 				if (!evaluator.Evaluate(source->dwords[index], value.dwords[index])) {
-					return false;
+					const bool reload_shader = evaluate_flat &&
+					    ((program.stage == ShaderType::Pixel &&
+					      (program.shader_hash == 0x06f5c0838a0b409bull ||
+					      program.shader_hash == 0xe1f1578df1050d8bull)) ||
+					     (program.stage == ShaderType::Compute &&
+					      program.shader_hash == 0x05021f35c669b111ull));
+					static std::atomic_bool logged_pixel {false};
+					static std::atomic_bool logged_compute {false};
+					auto& logged = program.stage == ShaderType::Pixel ? logged_pixel : logged_compute;
+					if (!reload_shader || !logged.exchange(true, std::memory_order_relaxed)) {
+						LOGF("SRT descriptor evaluation failed: shader=0x%016llx source=%u word=%u%s\n",
+						     static_cast<unsigned long long>(program.shader_hash), source_index, index,
+						     reload_shader ? "; using null descriptor" : "");
+						std::printf("SRT descriptor evaluation failed: shader=0x%016llx source=%u word=%u%s\n",
+						            static_cast<unsigned long long>(program.shader_hash), source_index, index,
+						            reload_shader ? "; using null descriptor" : "");
+						std::fflush(stdout);
+					}
+					if (!reload_shader) {
+						return false;
+					}
+					// On reload an unavailable descriptor cannot be used safely. Bind the
+					// existing null resource until the game supplies a readable one.
+					value.dwords.fill(0);
+					break;
 				}
 			}
 		}
@@ -1210,10 +1247,33 @@ bool EvaluateRuntimeSourcesImpl(const ResourcePlan& program, std::span<const uin
 			const bool clean    = read.flat_offset < clean_flat_slots.size() &&
 			                      clean_flat_slots[read.flat_offset] != 0u;
 			auto&      selected = clean ? clean_evaluator : evaluator;
-			if (read.flat_offset >= flattened.size() ||
-			    !selected.Evaluate(read.value, flattened[read.flat_offset])) {
+			if (read.flat_offset >= flattened.size()) {
 				return false;
 			}
+			if (!selected.Evaluate(read.value, flattened[read.flat_offset])) {
+				const bool reload_shader =
+				    (program.stage == ShaderType::Pixel &&
+				     (program.shader_hash == 0x06f5c0838a0b409bull ||
+				     program.shader_hash == 0xe1f1578df1050d8bull)) ||
+				    (program.stage == ShaderType::Compute &&
+				     program.shader_hash == 0x05021f35c669b111ull);
+				static std::atomic_bool logged_pixel {false};
+				static std::atomic_bool logged_compute {false};
+				auto& logged = program.stage == ShaderType::Pixel ? logged_pixel : logged_compute;
+				if (!reload_shader || !logged.exchange(true, std::memory_order_relaxed)) {
+					LOGF("SRT flat read failed: shader=0x%016llx slot=%u clean=%u%s\n",
+					     static_cast<unsigned long long>(program.shader_hash), read.flat_offset,
+					     clean ? 1u : 0u, reload_shader ? "; using zero" : "");
+					std::printf("SRT flat read failed: shader=0x%016llx slot=%u clean=%u%s\n",
+					            static_cast<unsigned long long>(program.shader_hash), read.flat_offset,
+					            clean ? 1u : 0u, reload_shader ? "; using zero" : "");
+					std::fflush(stdout);
+				}
+				if (!reload_shader) {
+					return false;
+				}
+				flattened[read.flat_offset] = 0;
+			}
 		}
 	}
 	results = std::move(evaluated);
@@ -1279,6 +1339,16 @@ static std::shared_ptr<LinearSrtPlan> BuildControlledLinearSrt(const ResourcePla
 void BuildLinearSrtPlan(ResourcePlan& program) {
     program.linear_srt.reset();
 #if defined(__x86_64__) || defined(_M_X64)
+    // Diagnose the reload failures with the checked evaluator rather than
+    // executing the linear plan. Keep all other shaders on the fast path.
+    const bool reload_pixel = program.stage == ShaderType::Pixel &&
+                              (program.shader_hash == 0x06f5c0838a0b409bull ||
+                              program.shader_hash == 0xe1f1578df1050d8bull);
+    const bool reload_compute = program.stage == ShaderType::Compute &&
+                                program.shader_hash == 0x05021f35c669b111ull;
+    if (reload_pixel || reload_compute) {
+        return;
+    }
     auto compiled=std::make_shared<LinearSrtPlan>();
     if (LinearSrtCompiler(program,*compiled).Build()) program.linear_srt=std::move(compiled);
     else program.linear_srt=BuildControlledLinearSrt(program);
diff --git a/src/libs/agc.cpp b/src/libs/agc.cpp
index 0111f02..6c3c1b5 100644
--- a/src/libs/agc.cpp
+++ b/src/libs/agc.cpp
@@ -44,6 +44,16 @@ namespace Libs::Graphics {
 
 static RenderContext* g_renderer = nullptr;
 
+// Keep diagnostics and frame timings, but avoid formatting routine AGC calls
+// on this performance test build. Full command traces remain on other builds.
+#if defined(KYTY_B32_QUIET_HOT_LOGS)
+#define AGC_PRINT_NAME() ((void)0)
+#define AGC_ROUTINE_LOG(...) ((void)0)
+#else
+#define AGC_PRINT_NAME() PRINT_NAME()
+#define AGC_ROUTINE_LOG(...) LOGF(__VA_ARGS__)
+#endif
+
 template <typename... Args>
 static void AgcTrace(const char* format, const Args&... args) {
 	if (Config::GraphicsDebugDumpEnabled()) {
@@ -336,7 +346,7 @@ struct Label {
 };
 
 int KYTY_SYSV_ABI AgcInit(uint32_t* state, uint32_t ver) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	LOGF("\t state = 0x%016" PRIx64 "\n"
 	     "\t ver   = %u\n",
@@ -352,7 +362,7 @@ int KYTY_SYSV_ABI AgcInit(uint32_t* state, uint32_t ver) {
 }
 
 void* KYTY_SYSV_ABI AgcGetRegisterDefaults2(uint32_t ver) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (ver > GRAPHICS_REGISTER_DEFAULTS_MAX_VERSION) {
 		LOGF_COLOR(Log::Color::Red, "\t unsupported version %u\n", ver);
@@ -361,7 +371,7 @@ void* KYTY_SYSV_ABI AgcGetRegisterDefaults2(uint32_t ver) {
 }
 
 void* KYTY_SYSV_ABI AgcGetRegisterDefaults2Internal(uint32_t ver) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (ver > GRAPHICS_REGISTER_DEFAULTS_MAX_VERSION) {
 		LOGF_COLOR(Log::Color::Red, "\t unsupported version %u\n", ver);
@@ -619,7 +629,7 @@ static void patch_shader_register_address(ShaderRegister* regs, uint32_t num_reg
 }
 
 int KYTY_SYSV_ABI AgcCreateShader(Shader** dst, void* header, const volatile void* code) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_NOT_IMPLEMENTED(dst == nullptr);
 	EXIT_NOT_IMPLEMENTED(header == nullptr);
@@ -688,7 +698,7 @@ int KYTY_SYSV_ABI AgcCreateShader(Shader** dst, void* header, const volatile voi
 
 int KYTY_SYSV_ABI AgcUnknownGetFusedShaderSize(SizeAlign* dst, const Shader* front,
                                                const Shader* back) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	LOGF("\t dst   = 0x%016" PRIx64 "\n"
 	     "\t front = 0x%016" PRIx64 "\n"
@@ -822,13 +832,13 @@ static int fuse_shader_halves(Shader* fused_result, const Shader* front,
 
 int KYTY_SYSV_ABI AgcUnknownFuseShaderHalves(Shader* fused_result, const Shader* front,
                                            const Shader* back, void* scratch_mem) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 	return fuse_shader_halves(fused_result, front, back, scratch_mem, true);
 }
 
 int KYTY_SYSV_ABI AgcUnknownNApJjpKNBl4(Shader* fused_result, const Shader* front,
                                       const Shader* back, void* scratch_mem) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 	return fuse_shader_halves(fused_result, front, back, scratch_mem, false);
 }
 
@@ -909,7 +919,7 @@ static int reg_indirect_patch_add_registers(uint32_t* cmd, uint32_t num_regs,
 
 int KYTY_SYSV_ABI AgcSetCxRegIndirectPatchSetAddress(uint32_t*                      cmd,
                                                      const volatile ShaderRegister* regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd  = 0x%016" PRIx64 "\n"
 	     "\t regs = 0x%016" PRIx64 "\n",
@@ -925,7 +935,7 @@ int KYTY_SYSV_ABI AgcSetCxRegIndirectPatchSetAddress(uint32_t*
 
 int KYTY_SYSV_ABI AgcSetShRegIndirectPatchSetAddress(uint32_t*                      cmd,
                                                      const volatile ShaderRegister* regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd  = 0x%016" PRIx64 "\n"
 	     "\t regs = 0x%016" PRIx64 "\n",
@@ -941,7 +951,7 @@ int KYTY_SYSV_ABI AgcSetShRegIndirectPatchSetAddress(uint32_t*
 
 int KYTY_SYSV_ABI AgcSetUcRegIndirectPatchSetAddress(uint32_t*                      cmd,
                                                      const volatile ShaderRegister* regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd  = 0x%016" PRIx64 "\n"
 	     "\t regs = 0x%016" PRIx64 "\n",
@@ -956,7 +966,7 @@ int KYTY_SYSV_ABI AgcSetUcRegIndirectPatchSetAddress(uint32_t*
 }
 
 int KYTY_SYSV_ABI AgcSetCxRegIndirectPatchSetNumRegisters(uint32_t* cmd, uint32_t num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd      = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -968,7 +978,7 @@ int KYTY_SYSV_ABI AgcSetCxRegIndirectPatchSetNumRegisters(uint32_t* cmd, uint32_
 }
 
 int KYTY_SYSV_ABI AgcSetShRegIndirectPatchSetNumRegisters(uint32_t* cmd, uint32_t num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd      = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -980,7 +990,7 @@ int KYTY_SYSV_ABI AgcSetShRegIndirectPatchSetNumRegisters(uint32_t* cmd, uint32_
 }
 
 int KYTY_SYSV_ABI AgcSetUcRegIndirectPatchSetNumRegisters(uint32_t* cmd, uint32_t num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd      = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -992,7 +1002,7 @@ int KYTY_SYSV_ABI AgcSetUcRegIndirectPatchSetNumRegisters(uint32_t* cmd, uint32_
 }
 
 int KYTY_SYSV_ABI AgcSetCxRegIndirectPatchAddRegisters(uint32_t* cmd, uint32_t num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd      = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -1004,7 +1014,7 @@ int KYTY_SYSV_ABI AgcSetCxRegIndirectPatchAddRegisters(uint32_t* cmd, uint32_t n
 }
 
 int KYTY_SYSV_ABI AgcSetShRegIndirectPatchAddRegisters(uint32_t* cmd, uint32_t num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd      = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -1016,7 +1026,7 @@ int KYTY_SYSV_ABI AgcSetShRegIndirectPatchAddRegisters(uint32_t* cmd, uint32_t n
 }
 
 int KYTY_SYSV_ABI AgcSetUcRegIndirectPatchAddRegisters(uint32_t* cmd, uint32_t num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cmd      = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -1047,7 +1057,7 @@ static uint32_t GraphicsPrimitiveTypeToGsOut(uint32_t prim_type) {
 
 int KYTY_SYSV_ABI AgcCreatePrimState(ShaderRegister* cx_regs, ShaderRegister* uc_regs,
                                      const Shader* hs, const Shader* gs, uint32_t prim_type) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cx_regs   = 0x%016" PRIx64 "\n"
 	     "\t uc_regs   = 0x%016" PRIx64 "\n"
@@ -1115,7 +1125,7 @@ int KYTY_SYSV_ABI AgcCreatePrimState(ShaderRegister* cx_regs, ShaderRegister* uc
 
 int KYTY_SYSV_ABI AgcUpdatePrimState(ShaderRegister* cx_regs, ShaderRegister* uc_regs,
                                      uint32_t prim_type) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t cx_regs   = 0x%016" PRIx64 "\n"
 	     "\t uc_regs   = 0x%016" PRIx64 "\n"
@@ -1174,7 +1184,7 @@ static GsOccupancyLimits get_gs_occupancy_limits(const Shader* gs, uint32_t vert
 
 int KYTY_SYSV_ABI AgcGetGsOversubscription(ShaderRegister* regs, const Shader* gs, uint32_t budget,
                                            float factor) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	constexpr uint32_t FULL_PC_OVERSUBSCRIPTION = 0x7ffu;
 	constexpr uint32_t FULL_SH_OVERSUBSCRIPTION = 0x007f0000u;
@@ -1320,7 +1330,7 @@ static void set_identity_interpolant_registers(ShaderRegister* regs, uint32_t fi
 
 int KYTY_SYSV_ABI AgcCreateInterpolantMapping(ShaderRegister* regs, const Shader* gs,
                                               const Shader* ps) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs = 0x%016" PRIx64 "\n"
 	     "\t gs   = 0x%016" PRIx64 "\n"
@@ -1376,7 +1386,7 @@ static uint32_t apply_interpolant_final_mask(uint32_t flags, uint32_t source,
 
 int KYTY_SYSV_ABI AgcCreateInterpolantMapping2(ShaderRegister* regs, const Shader* gs,
                                                const Shader* ps) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs = 0x%016" PRIx64 "\n"
 	     "\t gs   = 0x%016" PRIx64 "\n"
@@ -1451,11 +1461,11 @@ int KYTY_SYSV_ABI AgcCreateInterpolantMapping2(ShaderRegister* regs, const Shade
 }
 
 int KYTY_SYSV_ABI AgcGetDataPacketPayloadAddress(uint32_t** addr, uint32_t* cmd, int type) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	if (log_count.fetch_add(1) < 64) {
-		LOGF("\t addr = 0x%016" PRIx64 "\n"
+		AGC_ROUTINE_LOG("\t addr = 0x%016" PRIx64 "\n"
 		     "\t cmd  = 0x%016" PRIx64 "\n"
 		     "\t type = %d\n",
 		     reinterpret_cast<uint64_t>(addr), reinterpret_cast<uint64_t>(cmd), type);
@@ -1475,12 +1485,12 @@ int KYTY_SYSV_ABI AgcGetDataPacketPayloadAddress(uint32_t** addr, uint32_t* cmd,
 }
 
 int KYTY_SYSV_ABI AgcGetDataPacketPayloadRange(MemoryRange* range, uint32_t* cmd, int type) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	const bool                   should_log = (log_count.fetch_add(1) < 64);
 	if (should_log) {
-		LOGF("\t range = 0x%016" PRIx64 "\n"
+		AGC_ROUTINE_LOG("\t range = 0x%016" PRIx64 "\n"
 		     "\t cmd   = 0x%016" PRIx64 "\n"
 		     "\t type  = %d\n",
 		     reinterpret_cast<uint64_t>(range), reinterpret_cast<uint64_t>(cmd), type);
@@ -1506,7 +1516,7 @@ int KYTY_SYSV_ABI AgcGetDataPacketPayloadRange(MemoryRange* range, uint32_t* cmd
 	}
 
 	if (should_log) {
-		LOGF("\t cmd_id = 0x%08" PRIx32 ", len = %" PRIu32 "\n"
+		AGC_ROUTINE_LOG("\t cmd_id = 0x%08" PRIx32 ", len = %" PRIu32 "\n"
 		     "\t base   = 0x%016" PRIx64 "\n"
 		     "\t size   = 0x%016" PRIx64 "\n",
 		     cmd_id, len, reinterpret_cast<uint64_t>(range->m_base), range->m_size);
@@ -1530,9 +1540,9 @@ int KYTY_SYSV_ABI AgcWriteDataPatchSetAddressOrOffset(uint32_t* cmd, uint64_t ad
 
 int KYTY_SYSV_ABI AgcJumpPatchSetTarget(uint32_t* cmd, const volatile uint32_t* target,
                                         uint32_t size_in_dwords) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd            = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd            = 0x%016" PRIx64 "\n"
 	     "\t target         = 0x%016" PRIx64 "\n"
 	     "\t size_in_dwords = %" PRIu32 "\n",
 	     reinterpret_cast<uint64_t>(cmd), reinterpret_cast<uint64_t>(target), size_in_dwords);
@@ -1542,7 +1552,7 @@ int KYTY_SYSV_ABI AgcJumpPatchSetTarget(uint32_t* cmd, const volatile uint32_t*
 	auto cmd_id = cmd[0];
 	auto op     = (cmd_id >> 8u) & 0xffu;
 
-	LOGF("\t cmd_id         = 0x%08" PRIx32 ", op = 0x%02" PRIx32 ", len = %" PRIu32 "\n", cmd_id,
+	AGC_ROUTINE_LOG("\t cmd_id         = 0x%08" PRIx32 ", op = 0x%02" PRIx32 ", len = %" PRIu32 "\n", cmd_id,
 	     op, KYTY_PM4_LEN(cmd_id));
 
 	if (op != Pm4::IT_INDIRECT_BUFFER) {
@@ -1559,7 +1569,7 @@ int KYTY_SYSV_ABI AgcJumpPatchSetTarget(uint32_t* cmd, const volatile uint32_t*
 }
 
 int KYTY_SYSV_ABI AgcSuspendPoint() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_IF(g_renderer == nullptr);
 	g_renderer->GetGpu().Done();
@@ -1578,8 +1588,8 @@ int KYTY_SYSV_ABI AgcSuspendPoint() {
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbContextStateOp(CommandBuffer* buf, uint32_t operation) {
-	PRINT_NAME();
-	LOGF("\t operation = 0x%08" PRIx32 "\n", operation);
+	AGC_PRINT_NAME();
+	AGC_ROUTINE_LOG("\t operation = 0x%08" PRIx32 "\n", operation);
 
 	const auto size_dw = context_state_op_size_dw(operation);
 	if (buf == nullptr || size_dw == 0) {
@@ -1631,7 +1641,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbContextStateOp(CommandBuffer* buf, uint32_t operat
 }
 
 uint64_t KYTY_SYSV_ABI AgcDcbContextStateOpGetSize(uint32_t operation) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 	return static_cast<uint64_t>(context_state_op_size_dw(operation)) * sizeof(uint32_t);
 }
 
@@ -1654,26 +1664,26 @@ static uint32_t   g_workload_stream_mask = 0;
 static uint8_t    g_workload_streams[WORKLOAD_STREAM_MAX_ID + 1][WORKLOAD_STREAM_RECORD_SIZE] {};
 
 uint32_t KYTY_SYSV_ABI AgcDriverGetDefaultOwner() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 0x8a6c9018u;
 }
 
 uint32_t KYTY_SYSV_ABI AgcDriverGetResourceRegistrationMaxNameLength() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 0x8a6c9018u;
 }
 
 uint32_t KYTY_SYSV_ABI AgcDriverInitResourceRegistration() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 0x8a6c9018u;
 }
 
 uint32_t KYTY_SYSV_ABI
 AgcDriverQueryResourceRegistrationUserMemoryRequirements(uint64_t* size_in_bytes) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (size_in_bytes != nullptr) {
 		*size_in_bytes = 0;
@@ -1683,19 +1693,19 @@ AgcDriverQueryResourceRegistrationUserMemoryRequirements(uint64_t* size_in_bytes
 }
 
 int KYTY_SYSV_ABI AgcDriverRegisterOwner() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return static_cast<int>(0x8a6c9018u);
 }
 
 int KYTY_SYSV_ABI AgcDriverRegisterResource() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return static_cast<int>(0x8a6c9018u);
 }
 
 int KYTY_SYSV_ABI AgcDriverUnregisterOwnerAndResources(uint32_t owner_handle) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	(void)owner_handle;
 
@@ -1703,15 +1713,15 @@ int KYTY_SYSV_ABI AgcDriverUnregisterOwnerAndResources(uint32_t owner_handle) {
 }
 
 int KYTY_SYSV_ABI AgcDriverUnregisterResource() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return static_cast<int>(0x8a6c9018u);
 }
 
 int KYTY_SYSV_ABI AgcDriverRegisterWorkloadStream(uint32_t stream_id, const void* stream) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t stream_id = %" PRIu32 "\n"
+	AGC_ROUTINE_LOG("\t stream_id = %" PRIu32 "\n"
 	     "\t stream    = 0x%016" PRIx64 "\n",
 	     stream_id, reinterpret_cast<uint64_t>(stream));
 
@@ -1763,11 +1773,11 @@ uint32_t KYTY_SYSV_ABI AgcCbNopGetSize(uint32_t size_in_dwords) {
 uint32_t* KYTY_SYSV_ABI AgcCbDispatch(CommandBuffer* buf, uint32_t thread_group_x,
                                       uint32_t thread_group_y, uint32_t thread_group_z,
                                       uint32_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	if (log_count.fetch_add(1) < 64) {
-		LOGF("\t thread_group_x = %" PRIu32 "\n"
+		AGC_ROUTINE_LOG("\t thread_group_x = %" PRIu32 "\n"
 		     "\t thread_group_y = %" PRIu32 "\n"
 		     "\t thread_group_z = %" PRIu32 "\n"
 		     "\t modifier       = 0x%08" PRIx32 "\n",
@@ -1794,7 +1804,7 @@ uint32_t* KYTY_SYSV_ABI AgcCbDispatch(CommandBuffer* buf, uint32_t thread_group_
 }
 
 uint32_t KYTY_SYSV_ABI AgcCbDispatchGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 20;
 }
@@ -1805,9 +1815,9 @@ uint32_t* KYTY_SYSV_ABI AgcCbBranch(CommandBuffer* buf, uint8_t mode, uint8_t co
                                     const volatile uint32_t* buffer1, uint32_t size_in_dwords1,
                                     uint8_t cache_policy2, const volatile uint32_t* buffer2,
                                     uint32_t size_in_dwords2) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t mode             = 0x%02" PRIx8 "\n"
+	AGC_ROUTINE_LOG("\t mode             = 0x%02" PRIx8 "\n"
 	     "\t compare_function = 0x%02" PRIx8 "\n"
 	     "\t compare_addr     = 0x%016" PRIx64 "\n"
 	     "\t mask             = 0x%016" PRIx64 "\n"
@@ -1855,11 +1865,11 @@ uint32_t* KYTY_SYSV_ABI AgcCbBranch(CommandBuffer* buf, uint8_t mode, uint8_t co
 
 uint32_t* KYTY_SYSV_ABI AgcCbSetShRegisterRangeDirect(CommandBuffer* buf, uint32_t offset,
                                                       const uint32_t* values, uint32_t num_values) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	if (log_count.fetch_add(1) < 64) {
-		LOGF("\t buf        = 0x%016" PRIx64 "\n"
+		AGC_ROUTINE_LOG("\t buf        = 0x%016" PRIx64 "\n"
 		     "\t offset     = %" PRIx32 "\n"
 		     "\t values     = 0x%016" PRIx64 "\n"
 		     "\t num_values = %" PRIu32 "\n",
@@ -1889,9 +1899,9 @@ uint32_t* KYTY_SYSV_ABI AgcCbSetShRegisterRangeDirect(CommandBuffer* buf, uint32
 }
 
 uint32_t KYTY_SYSV_ABI AgcCbSetShRegisterRangeDirectGetSize(uint32_t num_values) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t num_values = %" PRIu32 "\n", num_values);
+	AGC_ROUTINE_LOG("\t num_values = %" PRIu32 "\n", num_values);
 
 	return 4u * num_values + 8u;
 }
@@ -1899,7 +1909,7 @@ uint32_t KYTY_SYSV_ABI AgcCbSetShRegisterRangeDirectGetSize(uint32_t num_values)
 uint32_t* KYTY_SYSV_ABI AgcCbSetShRegistersDirect(CommandBuffer*                 buf,
                                                   const volatile ShaderRegister* regs,
                                                   uint32_t                       num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs     = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -1966,7 +1976,7 @@ uint32_t* KYTY_SYSV_ABI AgcCbSetShRegistersDirect(CommandBuffer*
 uint32_t* KYTY_SYSV_ABI AgcCbSetUcRegistersDirect(CommandBuffer*                 buf,
                                                   const volatile ShaderRegister* regs,
                                                   uint32_t                       num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs     = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -2034,9 +2044,9 @@ uint32_t* KYTY_SYSV_ABI AgcCbSetUcRegistersDirect(CommandBuffer*
 }
 
 int KYTY_SYSV_ABI AgcDebugRaiseException(uint32_t exception_id) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t exception_id = 0x%08" PRIx32 "\n", exception_id);
+	AGC_ROUTINE_LOG("\t exception_id = 0x%08" PRIx32 "\n", exception_id);
 
 	return OK;
 }
@@ -2046,11 +2056,11 @@ uint32_t* KYTY_SYSV_ABI AgcCbReleaseMem(CommandBuffer* buf, uint8_t action, uint
                                         const volatile Label* address, uint8_t data_sel,
                                         uint64_t data, uint16_t gds_offset, uint16_t gds_size,
                                         uint8_t interrupt, uint32_t interrupt_ctx_id) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	if (log_count.fetch_add(1) < 64) {
-		LOGF("\t action           = 0x%02" PRIx8 "\n"
+		AGC_ROUTINE_LOG("\t action           = 0x%02" PRIx8 "\n"
 		     "\t gcr_cntl         = 0x%04" PRIx16 "\n"
 		     "\t dst              = %" PRIu8 "\n"
 		     "\t cache_policy     = 0x%02" PRIx8 "\n"
@@ -2112,15 +2122,15 @@ uint32_t* KYTY_SYSV_ABI AgcCbReleaseMem(CommandBuffer* buf, uint8_t action, uint
 }
 
 uint32_t KYTY_SYSV_ABI AgcCbQueueEndOfPipeActionGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 32;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcAcbResetQueue(CommandBuffer* buf, uint32_t op) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t op    = 0x%08" PRIx32 "\n", op);
+	AGC_ROUTINE_LOG("\t op    = 0x%08" PRIx32 "\n", op);
 
 	EXIT_NOT_IMPLEMENTED(buf == nullptr);
 	EXIT_NOT_IMPLEMENTED((op & ~0x1c2u) != 0);
@@ -2138,9 +2148,9 @@ uint32_t* KYTY_SYSV_ABI AgcAcbResetQueue(CommandBuffer* buf, uint32_t op) {
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbResetQueue(CommandBuffer* buf, uint32_t op, uint32_t state) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t op    = 0x%08" PRIx32 "\n"
+	AGC_ROUTINE_LOG("\t op    = 0x%08" PRIx32 "\n"
 	     "\t state = 0x%08" PRIx32 "\n",
 	     op, state);
 
@@ -2162,9 +2172,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbResetQueue(CommandBuffer* buf, uint32_t op, uint32
 uint32_t* KYTY_SYSV_ABI AgcDcbWaitUntilSafeForRendering(CommandBuffer* buf,
                                                         uint32_t       video_out_handle,
                                                         uint32_t       display_buffer_index) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t video_out_handle     = %" PRIu32 "\n"
+	AGC_ROUTINE_LOG("\t video_out_handle     = %" PRIu32 "\n"
 	     "\t display_buffer_index = %" PRIu32 "\n",
 	     video_out_handle, display_buffer_index);
 
@@ -2190,9 +2200,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbWaitUntilSafeForRendering(CommandBuffer* buf,
 uint32_t* KYTY_SYSV_ABI AgcDcbSetWorkloadsActive(CommandBuffer* buf, uint32_t stream_id,
                                                  const uint32_t* workload_ids,
                                                  uint32_t        workload_count) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t stream_id      = %" PRIu32 "\n"
+	AGC_ROUTINE_LOG("\t stream_id      = %" PRIu32 "\n"
 	     "\t workload_ids   = 0x%016" PRIx64 "\n"
 	     "\t workload_count = %" PRIu32 "\n",
 	     stream_id, reinterpret_cast<uint64_t>(workload_ids), workload_count);
@@ -2243,9 +2253,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetWorkloadsActive(CommandBuffer* buf, uint32_t st
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetWorkloadComplete(CommandBuffer* buf, uint32_t stream_id,
                                                   uint32_t workload_id) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t stream_id   = %" PRIu32 "\n"
+	AGC_ROUTINE_LOG("\t stream_id   = %" PRIu32 "\n"
 	     "\t workload_id = %" PRIu32 "\n",
 	     stream_id, workload_id);
 
@@ -2282,7 +2292,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetWorkloadComplete(CommandBuffer* buf, uint32_t s
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetShRegisterDirect(CommandBuffer* buf, ShaderRegister reg) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_NOT_IMPLEMENTED(buf == nullptr);
 
@@ -2303,7 +2313,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetShRegisterDirect(CommandBuffer* buf, ShaderRegi
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetCxRegisterDirect(CommandBuffer* buf, ShaderRegister reg) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_NOT_IMPLEMENTED(buf == nullptr);
 
@@ -2324,13 +2334,13 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetCxRegisterDirect(CommandBuffer* buf, ShaderRegi
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbSetCxRegisterDirectGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 12;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetUcRegisterDirect(CommandBuffer* buf, ShaderRegister reg) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_NOT_IMPLEMENTED(buf == nullptr);
 
@@ -2353,7 +2363,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetUcRegisterDirect(CommandBuffer* buf, ShaderRegi
 uint32_t* KYTY_SYSV_ABI AgcDcbSetCxRegistersIndirect(CommandBuffer*                 buf,
                                                      const volatile ShaderRegister* regs,
                                                      uint32_t                       num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs     = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -2377,7 +2387,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetCxRegistersIndirect(CommandBuffer*
 uint32_t* KYTY_SYSV_ABI AgcDcbSetShRegistersIndirect(CommandBuffer*                 buf,
                                                      const volatile ShaderRegister* regs,
                                                      uint32_t                       num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs     = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -2401,7 +2411,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetShRegistersIndirect(CommandBuffer*
 uint32_t* KYTY_SYSV_ABI AgcDcbSetUcRegistersIndirect(CommandBuffer*                 buf,
                                                      const volatile ShaderRegister* regs,
                                                      uint32_t                       num_regs) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t regs     = 0x%016" PRIx64 "\n"
 	     "\t num_regs = %" PRIu32 "\n",
@@ -2424,7 +2434,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetUcRegistersIndirect(CommandBuffer*
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetIndexSize(CommandBuffer* buf, uint8_t index_size,
                                            uint8_t cache_policy) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_size   = 0x%" PRIx8 "\n"
 	     "\t cache_policy = 0x%" PRIx8 "\n",
@@ -2445,7 +2455,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetIndexSize(CommandBuffer* buf, uint8_t index_siz
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetIndexBuffer(CommandBuffer* buf, uint64_t index_addr) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_addr = 0x%016" PRIx64 "\n", index_addr);
 
@@ -2466,7 +2476,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetIndexBuffer(CommandBuffer* buf, uint64_t index_
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetIndexCount(CommandBuffer* buf, uint32_t index_count) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_count = 0x%" PRIx32 "\n", index_count);
 
@@ -2485,7 +2495,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetIndexCount(CommandBuffer* buf, uint32_t index_c
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetNumInstances(CommandBuffer* buf, uint32_t num_instances) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t num_instances = 0x%" PRIx32 "\n", num_instances);
 
@@ -2508,7 +2518,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetNumInstances(CommandBuffer* buf, uint32_t num_i
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbSetNumInstancesGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 8;
 }
@@ -2581,7 +2591,7 @@ static void write_agc_internal_data_packet(uint32_t* cmd, uint32_t tag) {
 
 uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndex(CommandBuffer* buf, uint32_t index_count,
                                         const volatile void* index_addr, uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_count = 0x%" PRIx32 "\n"
 	     "\t index_addr  = 0x%016" PRIx64 "\n"
@@ -2611,7 +2621,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndex(CommandBuffer* buf, uint32_t index_count
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbDrawIndexGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 6u * 4u;
 }
@@ -2620,7 +2630,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexMultiInstanced(CommandBuffer* buf, uint32
                                                       const volatile void* index_addr,
                                                       const volatile void* object_ids,
                                                       uint32_t instance_count, uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_count    = 0x%" PRIx32 "\n"
 	     "\t index_addr     = 0x%016" PRIx64 "\n"
@@ -2658,7 +2668,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexMultiInstanced(CommandBuffer* buf, uint32
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbDrawIndexMultiInstancedGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 9u * 4u;
 }
@@ -2666,9 +2676,9 @@ uint32_t KYTY_SYSV_ABI AgcDcbDrawIndexMultiInstancedGetSize() {
 int KYTY_SYSV_ABI AgcUnknownIkfdtRIqCE(uint32_t* cmd, uint64_t arg1,
                                        const volatile uint32_t* target, uint32_t size_in_dwords,
                                        uint64_t arg4, uint64_t arg5) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd            = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd            = 0x%016" PRIx64 "\n"
 	     "\t arg1           = 0x%016" PRIx64 "\n"
 	     "\t target         = 0x%016" PRIx64 "\n"
 	     "\t size_in_dwords = %" PRIu32 "\n"
@@ -2696,7 +2706,7 @@ int KYTY_SYSV_ABI AgcUnknownIkfdtRIqCE(uint32_t* cmd, uint64_t arg1,
 
 uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexAuto(CommandBuffer* buf, uint32_t index_count,
                                             uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_count = 0x%" PRIx32 "\n"
 	     "\t modifier    = 0x%016" PRIx64 "\n",
@@ -2718,14 +2728,14 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexAuto(CommandBuffer* buf, uint32_t index_c
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbDrawIndexAutoGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 3u * 4u;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexOffset(CommandBuffer* buf, uint32_t index_offset,
                                               uint32_t index_count, uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t index_offset = 0x%" PRIx32 "\n"
 	     "\t index_count  = 0x%" PRIx32 "\n"
@@ -2750,16 +2760,16 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexOffset(CommandBuffer* buf, uint32_t index
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbDrawIndexOffsetGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 5u * 4u;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbSetBaseIndirectArgs(CommandBuffer* buf, uint32_t shader_type,
                                                   const volatile void* indirect_base_addr) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t shader_type        = %" PRIu32 "\n"
+	AGC_ROUTINE_LOG("\t shader_type        = %" PRIu32 "\n"
 	     "\t indirect_base_addr = 0x%016" PRIx64 "\n",
 	     shader_type, reinterpret_cast<uint64_t>(indirect_base_addr));
 
@@ -2783,7 +2793,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetBaseIndirectArgs(CommandBuffer* buf, uint32_t s
 
 uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexIndirect(CommandBuffer* buf, uint32_t data_offset_in_bytes,
                                                 uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t data_offset = 0x%" PRIx32 "\n"
 	     "\t modifier    = 0x%016" PRIx64 "\n",
@@ -2810,7 +2820,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexIndirect(CommandBuffer* buf, uint32_t dat
 
 uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndirect(CommandBuffer* buf, uint32_t data_offset_in_bytes,
                                            uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t data_offset = 0x%" PRIx32 "\n"
 	     "\t modifier    = 0x%016" PRIx64 "\n",
@@ -2841,7 +2851,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndirectMulti(CommandBuffer*       buf,
                                                 uint32_t             max_count_or_count,
                                                 const volatile void* count_addr,
                                                 uint32_t stride_in_bytes, uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t data_offset        = 0x%" PRIx32 "\n"
 	     "\t count_indirect     = 0x%" PRIx32 "\n"
@@ -2904,7 +2914,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexIndirectMulti(CommandBuffer*       buf,
                                                      uint32_t             max_count_or_count,
                                                      const volatile void* count_addr,
                                                      uint32_t stride_in_bytes, uint64_t modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t data_offset        = 0x%" PRIx32 "\n"
 	     "\t count_indirect     = 0x%" PRIx32 "\n"
@@ -2942,16 +2952,16 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDrawIndexIndirectMulti(CommandBuffer*       buf,
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbDrawIndirectGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 5u * 4u;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbDispatchIndirect(CommandBuffer* buf, uint32_t data_offset_in_bytes,
                                                uint32_t flags) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t data_offset = 0x%" PRIx32 "\n"
+	AGC_ROUTINE_LOG("\t data_offset = 0x%" PRIx32 "\n"
 	     "\t flags       = 0x%08" PRIx32 "\n",
 	     data_offset_in_bytes, flags);
 
@@ -2971,18 +2981,18 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDispatchIndirect(CommandBuffer* buf, uint32_t data
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbDispatchIndirectGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 3u * 4u;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbEventWrite(CommandBuffer* buf, uint8_t event_type,
                                          const volatile void* address) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	if (log_count.fetch_add(1) < 64) {
-		LOGF("\t event_type = 0x%02" PRIx8 "\n"
+		AGC_ROUTINE_LOG("\t event_type = 0x%02" PRIx8 "\n"
 		     "\t address    = 0x%016" PRIx64 "\n",
 		     event_type, reinterpret_cast<uint64_t>(address));
 	}
@@ -3015,7 +3025,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbEventWrite(CommandBuffer* buf, uint8_t event_type,
 }
 
 uint64_t KYTY_SYSV_ABI AgcDcbEventWriteGetSize(uint8_t event_type) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return (event_type & 0xfeu) == 0x38u ? 16u : 8u;
 }
@@ -3041,11 +3051,11 @@ uint32_t* KYTY_SYSV_ABI AgcAcbEventWrite(CommandBuffer* buf, uint8_t event_type,
 uint32_t* KYTY_SYSV_ABI AgcDcbAcquireMem(CommandBuffer* buf, uint8_t engine, uint32_t cb_db_op,
                                          uint32_t gcr_cntl, const volatile void* base,
                                          uint64_t size_bytes, uint32_t poll_cycles) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	static std::atomic<uint32_t> log_count {0};
 	if (log_count.fetch_add(1) < 64) {
-		LOGF("\t engine      = 0x%02" PRIx8 "\n"
+		AGC_ROUTINE_LOG("\t engine      = 0x%02" PRIx8 "\n"
 		     "\t cb_db_op    = 0x%08" PRIx32 "\n"
 		     "\t gcr_cntl    = 0x%08" PRIx32 "\n"
 		     "\t base        = 0x%016" PRIx64 "\n"
@@ -3109,7 +3119,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbAcquireMem(CommandBuffer* buf, uint8_t engine, uin
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbAcquireMemGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 8u * 4u;
 }
@@ -3121,16 +3131,16 @@ uint32_t* KYTY_SYSV_ABI AgcAcbAcquireMem(CommandBuffer* buf, uint32_t gcr_cntl,
 }
 
 uint32_t KYTY_SYSV_ABI AgcAcbAcquireMemGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return AgcDcbAcquireMemGetSize();
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbCondExec(CommandBuffer* buf, const volatile uint32_t* address,
                                        uint32_t num_dwords) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t address    = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t address    = 0x%016" PRIx64 "\n"
 	     "\t num_dwords = %" PRIu32 "\n",
 	     reinterpret_cast<uint64_t>(address), num_dwords);
 
@@ -3168,7 +3178,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbCondExec(CommandBuffer* buf, const volatile uint32
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbCondExecGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 20;
 }
@@ -3179,7 +3189,7 @@ uint32_t* KYTY_SYSV_ABI AgcAcbCondExec(CommandBuffer* buf, const volatile uint32
 }
 
 uint32_t KYTY_SYSV_ABI AgcAcbCondExecGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return AgcDcbCondExecGetSize();
 }
@@ -3217,7 +3227,7 @@ uint32_t* KYTY_SYSV_ABI AgcAcbWaitRegMem(CommandBuffer* buf, uint8_t size, uint8
 }
 
 uint64_t KYTY_SYSV_ABI AgcAcbWaitOnAddressGetSize(uint8_t size) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return AgcDcbWaitOnAddressGetSize(size);
 }
@@ -3294,7 +3304,7 @@ uint32_t* KYTY_SYSV_ABI AgcAcbCopyData(CommandBuffer* buf, uint8_t dst, uint8_t
 }
 
 uint64_t KYTY_SYSV_ABI AgcAcbCopyDataGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return AgcDcbCopyDataGetSize();
 }
@@ -3302,9 +3312,9 @@ uint64_t KYTY_SYSV_ABI AgcAcbCopyDataGetSize() {
 uint32_t* KYTY_SYSV_ABI AgcAcbDispatchIndirect(CommandBuffer*       buf,
                                                const volatile void* indirect_args,
                                                uint32_t             modifier) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t indirect_args = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t indirect_args = 0x%016" PRIx64 "\n"
 	     "\t modifier      = 0x%08" PRIx32 "\n",
 	     reinterpret_cast<uint64_t>(indirect_args), modifier);
 
@@ -3358,7 +3368,7 @@ uint32_t* KYTY_SYSV_ABI AgcAcbWriteData(CommandBuffer* buf, uint8_t dst, uint8_t
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbStallCommandBufferParser(CommandBuffer* buf) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_NOT_IMPLEMENTED(buf == nullptr);
 
@@ -3378,9 +3388,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbCopyData(CommandBuffer* buf, uint8_t dst, uint8_t
                                        uint64_t dst_address, uint8_t src, uint8_t src_cache_policy,
                                        uint64_t src_address_or_immediate, uint8_t item_size,
                                        uint8_t write_confirm) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t dst                      = 0x%02" PRIx8 "\n"
+	AGC_ROUTINE_LOG("\t dst                      = 0x%02" PRIx8 "\n"
 	     "\t dst_cache_policy         = 0x%02" PRIx8 "\n"
 	     "\t dst_address              = 0x%016" PRIx64 "\n"
 	     "\t src                      = 0x%02" PRIx8 "\n"
@@ -3418,7 +3428,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbCopyData(CommandBuffer* buf, uint8_t dst, uint8_t
 }
 
 uint64_t KYTY_SYSV_ABI AgcDcbCopyDataGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 6 * sizeof(uint32_t);
 }
@@ -3470,9 +3480,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbDmaData(CommandBuffer* buf, uint8_t engine, uint8_
 
 uint32_t* KYTY_SYSV_ABI AgcDcbJump(CommandBuffer* buf, uint8_t mode, uint8_t cache_policy,
                                    const uint32_t* target, uint32_t size_in_dwords) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t mode           = 0x%02" PRIx8 "\n"
+	AGC_ROUTINE_LOG("\t mode           = 0x%02" PRIx8 "\n"
 	     "\t cache_policy   = 0x%02" PRIx8 "\n"
 	     "\t target         = 0x%016" PRIx64 "\n"
 	     "\t size_in_dwords = %" PRIu32 "\n",
@@ -3500,21 +3510,21 @@ uint32_t* KYTY_SYSV_ABI AgcDcbJump(CommandBuffer* buf, uint8_t mode, uint8_t cac
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbJumpGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 16;
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbRewindGetSize() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return 8;
 }
 
 uint32_t* KYTY_SYSV_ABI AgcDcbRewind(CommandBuffer* buf, uint32_t initial_state) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t initial_state = 0x%08" PRIx32 "\n", initial_state);
+	AGC_ROUTINE_LOG("\t initial_state = 0x%08" PRIx32 "\n", initial_state);
 
 	if (buf == nullptr) {
 		return nullptr;
@@ -3535,9 +3545,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbRewind(CommandBuffer* buf, uint32_t initial_state)
 uint32_t* KYTY_SYSV_ABI AgcDcbSetPredication(CommandBuffer* buf, uint8_t condition, uint8_t op,
                                              uint8_t wait_op, const volatile void* address,
                                              uint32_t count_in_dwords) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t condition       = 0x%02" PRIx8 "\n"
+	AGC_ROUTINE_LOG("\t condition       = 0x%02" PRIx8 "\n"
 	     "\t op              = 0x%02" PRIx8 "\n"
 	     "\t wait_op         = 0x%02" PRIx8 "\n"
 	     "\t address         = 0x%016" PRIx64 "\n"
@@ -3570,9 +3580,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbSetPredication(CommandBuffer* buf, uint8_t conditi
 
 uint32_t* KYTY_SYSV_ABI AgcUnknownKRzWekV120(CommandBuffer* buf, uint32_t arg1, uint32_t arg2,
                                              uint32_t arg3) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t argc = 4\n"
+	AGC_ROUTINE_LOG("\t argc = 4\n"
 	     "\t arg0 = 0x%016" PRIx64 "\n"
 	     "\t arg1 = 0x%08" PRIx32 "\n"
 	     "\t arg2 = 0x%08" PRIx32 "\n"
@@ -3640,13 +3650,13 @@ uint32_t KYTY_SYSV_ABI AgcGetPacketSize(uint32_t* packet) {
 }
 
 int KYTY_SYSV_ABI AgcSetPacketPredication(uint32_t* packet, uint32_t predication) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t packet      = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t packet      = 0x%016" PRIx64 "\n"
 	     "\t predication = 0x%08" PRIx32 "\n",
 	     reinterpret_cast<uint64_t>(packet), predication);
 
-	LOGF("\t packet0     = 0x%08" PRIx32 ", op = 0x%02" PRIx32 ", r = 0x%02" PRIx32
+	AGC_ROUTINE_LOG("\t packet0     = 0x%08" PRIx32 ", op = 0x%02" PRIx32 ", r = 0x%02" PRIx32
 	     ", len = %" PRIu32 "\n",
 	     packet[0], (packet[0] >> 8u) & 0xffu, KYTY_PM4_R(packet[0]), AgcGetPacketSize(packet));
 
@@ -3657,9 +3667,9 @@ int KYTY_SYSV_ABI AgcSetPacketPredication(uint32_t* packet, uint32_t predication
 
 int KYTY_SYSV_ABI AgcSetRangePredication(uint32_t* start, const volatile uint32_t* end,
                                          uint32_t predication) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t start       = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t start       = 0x%016" PRIx64 "\n"
 	     "\t end         = 0x%016" PRIx64 "\n"
 	     "\t predication = 0x%08" PRIx32 "\n",
 	     reinterpret_cast<uint64_t>(start), reinterpret_cast<uint64_t>(end), predication);
@@ -3700,9 +3710,9 @@ int KYTY_SYSV_ABI AgcRewindPatchSetRewindState(uint32_t* cmd, uint8_t state) {
 }
 
 int KYTY_SYSV_ABI AgcCondExecPatchSetEnd(uint32_t* cmd, const volatile uint32_t* buffer) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd    = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd    = 0x%016" PRIx64 "\n"
 	     "\t buffer = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(cmd), reinterpret_cast<uint64_t>(buffer));
 
@@ -3732,9 +3742,9 @@ int KYTY_SYSV_ABI AgcCondExecPatchSetEnd(uint32_t* cmd, const volatile uint32_t*
 
 int KYTY_SYSV_ABI AgcCondExecPatchSetCommandAddress(uint32_t*                cmd,
                                                     const volatile uint32_t* command) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd     = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd     = 0x%016" PRIx64 "\n"
 	     "\t command = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(cmd), reinterpret_cast<uint64_t>(command));
 
@@ -3758,7 +3768,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbWriteData(CommandBuffer* buf, uint8_t dst, uint8_t
                                         uint64_t address_or_offset, const void* data,
                                         uint32_t num_dwords, uint8_t increment,
                                         uint8_t write_confirm) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t dst               = 0x%02" PRIx8 "\n"
 	     "\t cache_policy      = 0x%02" PRIx8 "\n"
@@ -3805,9 +3815,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbWriteData(CommandBuffer* buf, uint8_t dst, uint8_t
 }
 
 uint32_t KYTY_SYSV_ABI AgcDcbWriteDataGetSize(uint32_t num_dwords) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t num_dwords = %" PRIu32 "\n", num_dwords);
+	AGC_ROUTINE_LOG("\t num_dwords = %" PRIu32 "\n", num_dwords);
 
 	return 4u * num_dwords + 16u;
 }
@@ -3817,9 +3827,9 @@ uint32_t* KYTY_SYSV_ABI AgcDcbGetLodStats(CommandBuffer* buf, uint8_t cache_poli
                                           uint32_t buffer_size_in_bytes, uint32_t reset_count,
                                           uint8_t force_reset, uint8_t report_and_reset,
                                           uint32_t reporting_interval_in_100k_clocks) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cache_policy                      = 0x%02" PRIx8 "\n"
+	AGC_ROUTINE_LOG("\t cache_policy                      = 0x%02" PRIx8 "\n"
 	     "\t buffer                            = 0x%016" PRIx64 "\n"
 	     "\t buffer_size_in_bytes              = %" PRIu32 "\n"
 	     "\t reset_count                       = %" PRIu32 "\n"
@@ -3876,9 +3886,9 @@ static uint32_t* get_agc_wait_packet(uint32_t* cmd) {
 }
 
 int KYTY_SYSV_ABI AgcWaitRegMemPatchAddress(uint32_t* cmd, const volatile void* address) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd     = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd     = 0x%016" PRIx64 "\n"
 	     "\t address = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(cmd), reinterpret_cast<uint64_t>(address));
 
@@ -3900,9 +3910,9 @@ int KYTY_SYSV_ABI AgcWaitRegMemPatchAddress(uint32_t* cmd, const volatile void*
 }
 
 int KYTY_SYSV_ABI AgcWaitRegMemPatchReference(uint32_t* cmd, uint64_t reference) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd       = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd       = 0x%016" PRIx64 "\n"
 	     "\t reference = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(cmd), reference);
 
@@ -3919,11 +3929,11 @@ int KYTY_SYSV_ABI AgcWaitRegMemPatchReference(uint32_t* cmd, uint64_t reference)
 
 int KYTY_SYSV_ABI AgcQueueEndOfPipeActionPatchAddress(uint32_t*             cmd,
                                                       const volatile Label* address) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	// Not sure
 
-	LOGF("\t cmd     = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd     = 0x%016" PRIx64 "\n"
 	     "\t address = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(cmd), reinterpret_cast<uint64_t>(address));
 
@@ -3948,9 +3958,9 @@ int KYTY_SYSV_ABI AgcQueueEndOfPipeActionPatchAddress(uint32_t*             cmd,
 }
 
 int KYTY_SYSV_ABI AgcQueueEndOfPipeActionPatchData(uint32_t* cmd, uint64_t data) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t cmd  = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t cmd  = 0x%016" PRIx64 "\n"
 	     "\t data = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(cmd), data);
 
@@ -4011,7 +4021,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbWaitRegMem(CommandBuffer* buf, uint8_t size, uint8
                                          uint8_t op, uint8_t cache_policy,
                                          const volatile void* address, uint64_t reference,
                                          uint64_t mask, uint32_t poll_cycles) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t size             = 0x%02" PRIx8 "\n"
 	     "\t compare_function = 0x%02" PRIx8 "\n"
@@ -4088,7 +4098,7 @@ uint32_t* KYTY_SYSV_ABI AgcDcbWaitRegMem(CommandBuffer* buf, uint8_t size, uint8
 }
 
 uint64_t KYTY_SYSV_ABI AgcDcbWaitOnAddressGetSize(uint32_t size) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	switch (size) {
 		case 0: return 14u * 4u;
@@ -4158,7 +4168,7 @@ uint32_t* KYTY_SYSV_ABI AgcAcbPopMarker(CommandBuffer* buf) {
 uint32_t* KYTY_SYSV_ABI AgcDcbSetFlip(CommandBuffer* buf, uint32_t video_out_handle,
                                       int32_t display_buffer_index, uint32_t flip_mode,
                                       int64_t flip_arg) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t video_out_handle     = %" PRIu32 "\n"
 	     "\t display_buffer_index = %" PRId32 "\n"
@@ -4207,7 +4217,7 @@ static void submit_dcb(uint32_t* dcb, uint32_t size_in_dwords) {
 }
 
 int KYTY_SYSV_ABI AgcDriverSubmitDcb(const Packet* packet) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	EXIT_NOT_IMPLEMENTED(packet == nullptr);
 
@@ -4223,7 +4233,7 @@ int KYTY_SYSV_ABI AgcDriverSubmitDcb(const Packet* packet) {
 
 int KYTY_SYSV_ABI AgcDriverSubmitMultiDcbs(uint32_t* const* dcb_gpu_addrs,
                                            const uint32_t* dcb_sizes_in_dwords, uint32_t count) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	AgcTrace("\t count = %" PRIu32 "\n", count);
 
@@ -4255,9 +4265,11 @@ static void submit_acb(uint32_t queue, uint32_t* acb, uint32_t size_in_dwords) {
 		return;
 	}
 
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 	for (uint32_t i = 0; i < std::min<uint32_t>(size_in_dwords, 8); i++) {
-		LOGF("\t acb[%u] = 0x%08" PRIx32 "\n", i, acb[i]);
+		AGC_ROUTINE_LOG("\t acb[%u] = 0x%08" PRIx32 "\n", i, acb[i]);
 	}
+#endif
 
 	GraphicsDbgDumpDcb("a", size_in_dwords, acb);
 
@@ -4284,9 +4296,9 @@ static void submit_command_buffer(uint32_t queue, uint32_t* commands, uint32_t s
 }
 
 int KYTY_SYSV_ABI AgcDriverSubmitCommandBuffer(void* queue_context, const Packet* packet) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t queue_context = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t queue_context = 0x%016" PRIx64 "\n"
 	     "\t packet        = 0x%016" PRIx64 "\n",
 	     reinterpret_cast<uint64_t>(queue_context), reinterpret_cast<uint64_t>(packet));
 
@@ -4295,7 +4307,7 @@ int KYTY_SYSV_ABI AgcDriverSubmitCommandBuffer(void* queue_context, const Packet
 	}
 
 	const uint32_t queue = get_driver_queue(queue_context);
-	LOGF("\t queue = 0x%08" PRIx32 "\n"
+	AGC_ROUTINE_LOG("\t queue = 0x%08" PRIx32 "\n"
 	     "\t addr  = 0x%016" PRIx64 "\n"
 	     "\t size  = 0x%08" PRIx32 "\n"
 	     "\t flags = 0x%02" PRIx8 "\n",
@@ -4309,9 +4321,9 @@ int KYTY_SYSV_ABI AgcDriverSubmitMultiCommandBuffers(void*            queue_cont
                                                      uint32_t* const* command_buffers,
                                                      const uint32_t*  sizes_in_dwords,
                                                      uint32_t         count) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t queue_context = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t queue_context = 0x%016" PRIx64 "\n"
 	     "\t count         = %" PRIu32 "\n",
 	     reinterpret_cast<uint64_t>(queue_context), count);
 
@@ -4330,16 +4342,16 @@ int KYTY_SYSV_ABI AgcDriverSubmitMultiCommandBuffers(void*            queue_cont
 }
 
 int KYTY_SYSV_ABI AgcDriverSubmitAcb(uint32_t queue, const Packet* packet) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t queue  = 0x%08" PRIx32 "\n"
+	AGC_ROUTINE_LOG("\t queue  = 0x%08" PRIx32 "\n"
 	     "\t packet = 0x%016" PRIx64 "\n",
 	     queue, reinterpret_cast<uint64_t>(packet));
 
 	if (packet == nullptr) {
 		return OK;
 	}
-	LOGF("\t acb   = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t acb   = 0x%016" PRIx64 "\n"
 	     "\t size  = 0x%08" PRIx32 "\n"
 	     "\t flags = 0x%02" PRIx8 "\n",
 	     reinterpret_cast<uint64_t>(packet->addr), packet->dw_num, packet->flags);
@@ -4350,9 +4362,9 @@ int KYTY_SYSV_ABI AgcDriverSubmitAcb(uint32_t queue, const Packet* packet) {
 
 int KYTY_SYSV_ABI AgcDriverSubmitMultiAcbs(uint32_t queue, uint32_t* const* acbs,
                                            const uint32_t* sizes_in_dwords, uint32_t count) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
-	LOGF("\t queue = 0x%08" PRIx32 "\n"
+	AGC_ROUTINE_LOG("\t queue = 0x%08" PRIx32 "\n"
 	     "\t count = %" PRIu32 "\n",
 	     queue, count);
 
@@ -4371,7 +4383,7 @@ int KYTY_SYSV_ABI AgcDriverSubmitMultiAcbs(uint32_t queue, uint32_t* const* acbs
 }
 
 int KYTY_SYSV_ABI AgcDriverAddEqEvent(LibKernel::EventQueue::KernelEqueue eq, int id, void* udata) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (eq == LibKernel::EventQueue::KERNEL_EQUEUE_INVALID) {
 		return LibKernel::KERNEL_ERROR_EBADF;
@@ -4382,7 +4394,7 @@ int KYTY_SYSV_ABI AgcDriverAddEqEvent(LibKernel::EventQueue::KernelEqueue eq, in
 }
 
 int KYTY_SYSV_ABI AgcDriverDeleteEqEvent(LibKernel::EventQueue::KernelEqueue eq, int id) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (eq == LibKernel::EventQueue::KERNEL_EQUEUE_INVALID) {
 		return LibKernel::KERNEL_ERROR_EBADF;
@@ -4393,7 +4405,7 @@ int KYTY_SYSV_ABI AgcDriverDeleteEqEvent(LibKernel::EventQueue::KernelEqueue eq,
 }
 
 int KYTY_SYSV_ABI AgcDriverGetEqEventType(const LibKernel::EventQueue::KernelEvent* ev) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (ev == nullptr) {
 		return 0;
@@ -4407,7 +4419,7 @@ int KYTY_SYSV_ABI AgcDriverGetEqEventType(const LibKernel::EventQueue::KernelEve
 }
 
 uint32_t KYTY_SYSV_ABI AgcDriverGetEqContextId(const LibKernel::EventQueue::KernelEvent* ev) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	if (ev == nullptr) {
 		return 0;
@@ -4421,12 +4433,12 @@ uint32_t KYTY_SYSV_ABI AgcDriverGetEqContextId(const LibKernel::EventQueue::Kern
 }
 
 int KYTY_SYSV_ABI AgcDriverSetTFRing(const volatile void* base, uint32_t size) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	g_tessellation_driver_state.tf_ring_base = reinterpret_cast<uint64_t>(base);
 	g_tessellation_driver_state.tf_ring_size = size;
 
-	LOGF("\t base = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t base = 0x%016" PRIx64 "\n"
 	     "\t size = 0x%08" PRIx32 "\n",
 	     g_tessellation_driver_state.tf_ring_base, g_tessellation_driver_state.tf_ring_size);
 
@@ -4434,13 +4446,13 @@ int KYTY_SYSV_ABI AgcDriverSetTFRing(const volatile void* base, uint32_t size) {
 }
 
 int KYTY_SYSV_ABI AgcDriverSetHsOffchipParam(uint64_t value0, uint64_t value1, uint64_t value2) {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	g_tessellation_driver_state.hs_offchip_value0 = value0;
 	g_tessellation_driver_state.hs_offchip_value1 = value1;
 	g_tessellation_driver_state.hs_offchip_value2 = value2;
 
-	LOGF("\t value0 = 0x%016" PRIx64 "\n"
+	AGC_ROUTINE_LOG("\t value0 = 0x%016" PRIx64 "\n"
 	     "\t value1 = 0x%016" PRIx64 "\n"
 	     "\t value2 = 0x%016" PRIx64 "\n",
 	     value0, value1, value2);
@@ -4449,13 +4461,13 @@ int KYTY_SYSV_ABI AgcDriverSetHsOffchipParam(uint64_t value0, uint64_t value1, u
 }
 
 bool KYTY_SYSV_ABI AgcDriverIsCaptureInProgress() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return false;
 }
 
 int KYTY_SYSV_ABI AgcDriverUnknownU9ueyEhSkF4() {
-	PRINT_NAME();
+	AGC_PRINT_NAME();
 
 	return static_cast<int>(0x8a6c9018u);
 }
diff --git a/src/libs/ajm.cpp b/src/libs/ajm.cpp
index e132ed0..d26f4e0 100644
--- a/src/libs/ajm.cpp
+++ b/src/libs/ajm.cpp
@@ -1195,12 +1195,14 @@ int KYTY_SYSV_ABI AjmBatchJobRunSplit(AjmBatchInfo* info, uint32_t instance, uin
 	(void)AjmGetInstanceFormat(instance, nullptr, &decoder, &gapless);
 	AjmWriteSideband(flags, sideband_output, sideband_output_size, decoder, gapless, decode_result);
 
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 	LOGF("\t instance = 0x%08" PRIx32 ", flags = 0x%016" PRIx64 ", input_buffers = 0x%016" PRIx64
 	     ", input_num = 0x%016" PRIx64 ", output_buffers = 0x%016" PRIx64
 	     ", output_num = 0x%016" PRIx64 "\n",
 	     instance, flags, reinterpret_cast<uint64_t>(input_buffers),
 	     static_cast<uint64_t>(input_buffers_num), reinterpret_cast<uint64_t>(output_buffers),
 	     static_cast<uint64_t>(output_buffers_num));
+#endif
 	return AjmAppendJob(info, 16 * (input_buffers_num + output_buffers_num) + 32, "run-split");
 }
 
diff --git a/src/libs/ajm/atrac9_decoder.h b/src/libs/ajm/atrac9_decoder.h
index b6aab05..b72d442 100644
--- a/src/libs/ajm/atrac9_decoder.h
+++ b/src/libs/ajm/atrac9_decoder.h
@@ -303,10 +303,12 @@ private:
 		          static_cast<uint32_t>(m_codec_info.samplingRate), m_sample_encoding);
 		m_pcm_buffer.assign(FrameOutputBytes(), 0);
 
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 		LOGF("AJM ATRAC9 initialized: %d Hz, %d ch, frame_samples=%d, superframe=%d bytes/%d "
 		     "frames\n",
 		     m_codec_info.samplingRate, m_codec_info.channels, m_codec_info.frameSamples,
 		     m_codec_info.superframeSize, m_codec_info.framesInSuperframe);
+#endif
 
 		return true;
 	}
@@ -493,8 +495,10 @@ private:
 					gapless_decode.skip_samples  = static_cast<uint16_t>(std::min<uint32_t>(
 					    AjmReadLe32(input + payload + 4), std::numeric_limits<uint16_t>::max()));
 					gapless->Set(gapless_decode, true);
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 					LOGF("AJM ATRAC9 gapless: total=%" PRIu32 ", skip=%" PRIu16 "\n",
 					     gapless_decode.total_samples, gapless_decode.skip_samples);
+#endif
 				}
 			} else if (AjmFourCcEquals(chunk, 'd', 'a', 't', 'a')) {
 				*data_offset                  = payload;
diff --git a/src/libs/libPlayGo.cpp b/src/libs/libPlayGo.cpp
index 94f0093..9ee53e3 100644
--- a/src/libs/libPlayGo.cpp
+++ b/src/libs/libPlayGo.cpp
@@ -142,9 +142,11 @@ int KYTY_SYSV_ABI PlayGoClose(int handle) {
 
 int KYTY_SYSV_ABI PlayGoGetLocus(int handle, const uint16_t* chunk_ids, uint32_t number_of_entries,
                                  int8_t* out_loci) {
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 	PRINT_NAME();
 
 	LOGF("\t handle = %d\n", handle);
+#endif
 
 	if (auto ret = validate_handle(handle); ret != OK) {
 		return ret;
@@ -158,7 +160,9 @@ int KYTY_SYSV_ABI PlayGoGetLocus(int handle, const uint16_t* chunk_ids, uint32_t
 	ensure_chunks_loaded();
 
 	for (uint32_t i = 0; i < number_of_entries; i++) {
+#if !defined(KYTY_B32_QUIET_HOT_LOGS)
 		LOGF("\t chunk_ids[%u] = %" PRIu16 "\n", i, chunk_ids[i]);
+#endif
 
 		if (is_valid_chunk(chunk_ids[i])) {
 			out_loci[i] = PLAYGO_LOCUS_LOCAL_FAST;
